# 13. Phase 1 구축 계획

**작성일**: 2026-08-07 · Claude Code로 만듭니다.
[12](12-page-level-grading.md)의 설계 전환과 실측 결과를 반영한 Phase 1 범위입니다.
[07 로드맵](07-roadmap.md)의 Phase 1 절을 이 문서가 대체합니다.

---

## 13.1 Python 워커가 사라집니다

[03 §3](03-architecture.md)에서 이미지 처리 워커를 Python + OpenCV로 분리했습니다.
**이유는 원근 보정·마커 정합·crop이었고, 그게 전부 없어졌습니다.**

지금 파이프라인에 이미지 연산이라고 할 만한 것은 두 가지뿐입니다.

| 하는 일 | 필요한 것 |
|---|---|
| 사진 크기를 2576px로 줄여 API에 보냄 | 아무 이미지 라이브러리 |
| (선택) 머리말 마스킹 | 사각형 하나 칠하기 |

**OpenCV도, 별도 서비스도, 다른 언어도 필요 없습니다.**
나머지는 전부 API 호출과 규칙 판정이라 TypeScript 한 곳에서 끝납니다.

| 영역 | 선택 | 바뀐 점 |
|---|---|---|
| 웹 + API | Next.js (App Router) + TypeScript + Tailwind | 유지 |
| ~~이미지 워커~~ | ~~Python + OpenCV~~ | 🛑 **삭제** |
| 백그라운드 처리 | Next.js Route Handler + Postgres `jobs` | 유지 (언어만 통일) |
| DB / 스토리지 / 인증 | Supabase | 유지 |
| AI 채점 | Claude API (`@anthropic-ai/sdk`) | 유지 |
| ~~pdf.js~~ | | 🛑 **삭제** — 템플릿 편집기가 없어졌으므로 |
| 배포 | Vercel + Supabase | 워커 호스팅 불필요 |

**서비스 하나, 언어 하나, 배포 대상 하나.** `tools/`의 파이썬은 실험 도구로 남고
제품에는 들어가지 않습니다 — 프롬프트와 스키마만 TypeScript로 옮깁니다.

### Vercel Pro · Supabase Pro 기준 (2026-08-07 확인)

| 조건 | 설계에 미치는 영향 |
|---|---|
| Vercel Pro — 함수 최대 300초 | **한 장에 20~40초라 장당 호출은 여유. 배치를 한 요청에 담는 건 불가** |
| Supabase Pro — 무활동 중지 없음 | 방학처럼 뜸한 기간에도 첫 요청이 안 죽음 |
| Supabase Pro — 저장소 100GB | 사진 보관 정책이 필요 (§13.7) |

첫 줄이 큐 설계를 정합니다. **30장을 한 요청에서 처리하면 300초를 넘겨 죽습니다.**
장 하나에 호출 하나로 쪼개야 하고, 그러면 40초짜리라 넉넉합니다.

---

## 13.2 무엇을 만들지 — 조교의 하루로 정의합니다

지금 조교가 하는 일:

```
시험지 걷음 → 선생님이 채점 → 오답 정리 → 재시험 명단 만듦 → 학생에게 통보
```

Phase 1이 바꾸는 것:

```
시험지 걷음 → 사진 찍어 올림 → (자동 채점) → 선생님이 화면에서 확인·수정 → 확정
                                                  ↓
                                   재시험 명단 · 오답 목록이 그대로 나옴
```

**"선생님이 채점한다"가 "선생님이 확인한다"로 바뀌는 것**이 전부입니다.
초기에는 전 장을 확인하므로 자동 확정이 없습니다. 그래도 백지에서 채점하는 것보다
훨씬 빠르고, **그 확인 기록이 곧 정확도 데이터**가 됩니다([12 §12.8](12-page-level-grading.md)).

---

## 13.3 마일스톤 — 각 단계가 그 자체로 쓸모 있어야 합니다

### M1 · 한 반의 한 시험을 끝까지 (최우선)

> 🛑 **"한 반의 한 시험"이라는 묶음은 §13.9에서 없어졌습니다.**
> 학생마다 보는 시험이 달라서, 단위는 반이 아니라 **답안지 한 벌**입니다.
> 아래 항목들은 그대로 유효하고 묶는 방식만 바뀌었습니다.

**이것만으로 실제 수업에서 써볼 수 있어야 합니다.**

```
✅ 사진 여러 장 업로드 (모바일 웹, 카메라 직접 촬영 포함)
✅ 자동 채점 — 전사 → 판정 → PASS/FAIL
✅ 진행 표시 (30장이면 2~3분)
✅ 검수 화면 — 문항별 결과를 보고 고치고 확정
✅ 결과 화면 — PASS/FAIL 명단, 학생별 오답 목록
✅ 결과 내보내기 (복사·인쇄)
```

**의도적으로 안 넣는 것**

| 안 넣음 | 왜 |
|---|---|
| 반·학생 명단 등록 | **학생 이름을 머리말에서 읽습니다.** 명단 없이도 결과가 나옵니다 |
| 계정·권한 | 처음엔 한 계정으로. 여러 명 동시 사용은 M2 |
| 시험지 등록 | 없앤 것이 이 설계의 핵심입니다 |
| 자동 확정 | 1~2주차는 전 장 검수 |
| 정확도 대시보드 | 검수 기록만 쌓아두고 화면은 M3 |

명단 등록을 뺀 것이 중요합니다. **학원 명단 데이터를 기다리지 않고 시작할 수 있고**,
읽어낸 이름이 얼마나 정확한지도 M1에서 저절로 확인됩니다.

### M2 · 여러 명이 동시에 쓰기

```
✅ 교사·조교 계정 (Supabase Auth)
✅ 반·학생 명단 — 읽어낸 이름을 명단에 붙여 학생 ID로 정규화
✅ 시험 이력 — 같은 학생의 지난 결과를 봄
✅ 이름은 저장하지 않고 학생 ID로 치환 ([12 §12.9](12-page-level-grading.md))
```

### M3 · 검수를 줄이기

```
✅ 확신도 · 경계선(커트라인 ±2) · 밀림 경보 기반 라우팅
✅ 정확도 대시보드 — 선생님이 고친 것과 그대로 둔 것을 누적 집계
✅ 채점 규칙 설정 — 철자 엄격/관대를 시험 단위로
✅ 흔들리는 칸 검출 (같은 사진 2회 읽어 불일치 시 검수)
```

**M3의 근거는 M1·M2가 쌓은 데이터입니다.** 순서를 바꿀 수 없습니다.

### M4 · 이후

오답노트 자동 생성, 재시험지 생성, 학부모 리포트, 통계.

---

## 13.4 큐 — M1은 브라우저가 몰아줍니다

> 🛑 아래 그림의 `jobs` 표와 "업로드 → 30개 생성"은 §13.9에서 바뀌었습니다.
> 일감의 단위는 `sheets`의 한 행이고, 30개를 한꺼번에 만들지 않고
> **접수할 때마다 하나씩** 생깁니다. 브라우저가 몬다는 뼈대는 그대로입니다.

장당 호출로 쪼개야 하므로 누군가 여러 번 불러야 합니다.
**M1에서는 브라우저가 그 역할을 합니다.**

```
업로드 → job 행 30개 생성 → 즉시 응답
   ↓
브라우저가 /api/grade?job=N 을 동시 4개씩 호출
   ↓
끝난 것부터 화면에 채워짐 (진행률이 저절로 나옴)
```

크론도, 별도 워커도, 상태 머신도 필요 없습니다. **브라우저를 닫아도 job 행은
남아 있으므로** 다시 열면 이어서 처리합니다.

나중에 필요해지면(조교가 올려두고 나가는 흐름) Vercel Cron으로 미처리 job을
쓸어담는 것을 M2에 붙입니다. **지금 넣으면 쓰지도 않을 것을 디버깅하게 됩니다.**

---

## 13.4b M1에서 먼저 정해야 할 것

### 🔴 채점 규칙 — 철자를 엄격히 볼 것인가

선생님들은 지금 한 글자 오타를 **정답 처리합니다**
([12 §12.6](12-page-level-grading.md), 다섯 건 확인).

| 선택 | 결과 |
|---|---|
| 관대 (현재 기본) | 선생님과 잘 맞음. 대신 철자 오류가 그대로 통과 |
| 엄격 | 선생님보다 정확할 수 있음. 대신 '우리만 오답'이 늘어 초기 검수가 늘어남 |

**시스템이 정할 수 없는 교육 방침입니다.** M1은 관대를 기본값으로 두되
**조교 화면에서 바꿀 수 있게** 만들고, 실제 운영에서 정합니다.
(시험 단위로 두려 했으나 §13.9에서 시험 묶음 자체가 없어졌습니다.)

### 학생 이름 처리

머리말에서 읽되 **DB에는 학생 ID로만 저장**합니다. M1은 명단이 없으므로
이름을 임시로 들고 있다가, M2에서 명단이 붙으면 치환하고 버립니다.

---

## 13.8 현장 테스트에서 나온 것 (2026-08-07, 1단계)

배포 직후 실제 시험지 한 장으로 눌러봤습니다. **제한이라고 여긴 두 가지가
가장 값진 결과였습니다.**

### 🔴 양면 인쇄 시험지 — 설계에 없던 것

이 학원 시험지 중 **양면 인쇄된 것**이 있습니다. 앞면만 찍으면 60문항 중
47문항만 채점됩니다. 그리고 그대로 두면 이렇게 됩니다.

```
인쇄 60문항 · 촬영 47문항 · 그중 오답 3개 · 커트라인 -8
→ verdict(3, 8) = PASS
   안 찍힌 뒷면 13문항을 통째로 틀린 학생도 집에 갑니다.
```

**이건 버그였습니다.** 마침 커트라인이 빨간펜에 가려 판정이 안 나와서 드러나지
않았을 뿐입니다. 고친 것:

| 고침 | 내용 |
|---|---|
| 경보 등급 분리 | `drift`(밀림) / **`incomplete`(일부만 찍힘)** / `info`(구조 안내) |
| 판정 차단 | `incomplete`면 **PASS/FAIL을 내지 않습니다** |
| 문구 | "밀렸거나 빠졌습니다" → "60문항인데 47문항만 찍혔습니다(47번까지). **뒷면을 마저 찍어 함께 올리십시오**" |
| 여러 장 병합 | 한 학생의 여러 장을 **문항 번호로 정렬해 합칩니다.** 순서를 사람이 맞출 필요가 없습니다 |

원인이 다르면 조교가 할 일도 다릅니다. 밀림은 다시 찍어야 하고, 페이지 누락은
**한 장 더** 찍으면 됩니다. 같은 문구로 뭉뚱그리면 안 됩니다.

### 커트라인이 가려질 수 있다

채점 표시가 머리말을 덮어 커트라인을 못 읽었습니다. 판정을 안 낸 것은 맞는
동작이지만, 그러면 조교가 막힙니다.

→ **커트라인 직접 입력**을 폴백으로 넣었습니다. 다만 **시험 하나에 한 번**입니다 —
한 반 학생이 전부 같은 커트라인을 쓰므로 장마다 넣는 일이 아닙니다.
"조교 입력 0"이라는 원칙을 깨지 않는 선입니다.

### 2차 — 문법백지TEST 2장 (같은 날)

| | |
|---|---|
| 2쪽 · 빈칸 48칸 | 89초 · **$0.299** |
| 시스템이 읽은 칸 | 46 (2칸 못 읽음) |
| **틀린 문항** | **실제와 정확히 일치** |
| 커트라인 -6, 오답 3 | **PASS — 실제와 동일** |

**여기서 판정 차단이 과하다는 게 드러났습니다.** 2칸을 못 읽었다고 PASS/FAIL을
막았는데, 따져보면 막을 이유가 없었습니다.

```
못 읽은 2칸이 전부 오답이었다 쳐도 → 오답 5개 ≤ 허용 6개 → 여전히 PASS
```

**결정이 안 바뀝니다.** 반면 1차(60칸 중 47칸)는 다릅니다.

```
못 읽은 13칸이 전부 오답이었다 쳐도 → 오답 16개 > 허용 8개 → FAIL로 뒤집힘
```

→ 규칙을 바꿨습니다. **"덜 읽혔으면 막는다"가 아니라 "못 읽은 칸이 전부 틀렸다고
가정해도 결정이 바뀌면 막는다"**입니다. [12.8](12-page-level-grading.md)의 경계선
검수와 같은 발상입니다 — 불확실성이 결정을 뒤집을 수 있을 때만 사람을 부릅니다.

경보 문구도 고쳤습니다. **2장을 다 올렸는데 "뒷면을 마저 올리십시오"는 틀린 안내**입니다.

> ~~뒷면이나 다음 장을 마저 찍어 함께 올리십시오~~
> **2장을 올렸는데 48문항 중 46문항만 읽혔습니다 (46번까지, 2칸 부족).
> 장이 더 있는지 확인하시고, 다 올리셨다면 2칸을 놓친 것이니 검수해 주십시오.**

커트라인 비율(`-10%`)도 **인쇄된 전체 문항 기준**으로 세도록 고쳤습니다.
읽힌 칸만으로 세면 덜 읽힐수록 커트라인이 낮아져 통과가 쉬워집니다.

### 🔴 사진이 90도 누워 있었습니다 — 2칸 누락의 유력한 원인

올린 사진 두 장 다 **답안지가 가로로 누워** 있었습니다. 글자가 세로로 서 있고
종이가 옆으로 돌아간 상태입니다. [08](08-sample-analysis.md)에서 위험으로 잡아둔
"90도 회전 촬영"이 실사용에서 그대로 나왔습니다.

**그런데도 48칸 중 46칸을 읽고 오답을 정확히 맞혔습니다.**
회전은 **막는 요인이 아니라 깎아먹는 요인**입니다. 못 읽은 2칸이 그 대가일
가능성이 큽니다.

→ 사진마다 **↺ ↻ 돌리기 버튼**을 넣고, 가로로 누운 사진에는 테두리를 노랗게
칠하고 경고를 띄웁니다. **자동으로 돌리지는 않습니다** — 진짜 가로 시험지가
있을 수 있어 사람이 보고 정합니다.

### 이 시험지에서는 번호 검증이 힘을 못 씁니다

문법백지TEST의 빈칸에는 **번호가 인쇄돼 있지 않습니다.** 모델이 번호를 스스로
붙이므로 46칸을 읽으면 언제나 `1..46`으로 연속입니다.
→ **번호 연속성 검사가 무력합니다.**

2칸 부족이 잡힌 것은 오직 머리말의 `점 수 [ /48 ]` 덕분이었습니다.
이 유형에서는 **`printedTotal`이 유일한 검증 수단**입니다. 머리말이 가려지거나
없는 시험지라면 2칸이 빠진 것을 **아무도 모릅니다.**

| 시험지 유형 | 번호 연속성 | 첫 글자 | 언어 | 총 문항 수 |
|---|:--:|:--:|:--:|:--:|
| 단어시험 (번호 인쇄) | ✅ | ✅ | ✅ | ✅ |
| **문법 빈칸 (번호 없음)** | ❌ | ❌ | 🟡 | ✅ **유일** |

### 머리말 병합이 맞게 동작했습니다

2쪽 머리말은 학생명·점수·커트라인이 **전부 공란**이었습니다.
병합이 1쪽 값을 가져왔고, 문항 수는 큰 값(48)을 취했습니다. 설계대로입니다.

### 🟢 문법백지TEST 유형 판단을 정정합니다

[12 §12.12](12-page-level-grading.md)에서 문법 시험지를 대상 밖 후보로 뒀는데,
**빈칸 채우기형은 잘 됩니다.** 48칸 중 46칸을 읽고 틀린 문항이 정확히 일치했습니다.

Phase 0에서 걸렸던 것은 **자유 나열형**(`1) 동명사만 쓰는 동사: enjoy, finish…`)
한 부분이었습니다. 그건 여전히 대상 밖이지만, **문법 시험지 전체가 아닙니다.**

| 유형 | 판단 |
|---|---|
| 단어시험 (표·밑줄·첫글자) | ✅ 대상 |
| **문법 빈칸 채우기** | ✅ **대상** ← 이번에 확인 |
| 자유 나열형 | ❌ 대상 밖 — 붙일 앵커가 없음 |
| 서술형·직독직해 | ❌ 대상 밖 |

### 3차 — 단어시험 2장 (같은 날)

| | |
|---|---|
| 2쪽 · 50문항 · 커트라인 -7 | 79초 · **$0.274** |
| 전 문항 인지 | ✅ 부족분 없음 |
| 사람 채점 | 오답 **5개** |
| 시스템 채점 | 오답 **7개** (사람이 놓친 2개를 더 잡음) |
| 결과 | **PASS** (7 ≤ 7) |

### 비용과 시간이 두 번 측정으로 수렴합니다

| | 쪽 | 문항 | 시간 | 비용 | 쪽당 |
|---|--:|--:|--:|--:|--:|
| 2차 문법백지 | 2 | 48 | 89초 | $0.299 | $0.150 |
| 3차 단어시험 | 2 | 50 | 79초 | $0.274 | $0.137 |

**쪽당 $0.14 · 40초**로 보면 됩니다. Phase 0의 실측 추정($0.118/장)보다 조금
비싼데, 판정 호출이 한 번 더 붙기 때문입니다.

| 규모 | 비용 | 시간(학생 4명씩 동시 처리) |
|---|--:|--:|
| 한 반 20명 · 2쪽 | **$5.6** (약 8천 원) | 약 6분 30초 |
| 하루 300쪽 | **$42** | — |

**한 반에 8천 원**입니다. 선생님이 한 반 채점에 쓰는 시간을 생각하면 문제가
되는 금액이 아닙니다. 다만 **하루 300쪽을 매일 돌리면 월 120만 원 선**이라
M2에서 Sonnet 5 전환을 검토할 근거는 됩니다 — 검수 화면이 생겨 회귀를
귀속시킬 수 있게 된 뒤입니다.

이 숫자가 **큐 설계를 정합니다.** 한 반이 6분 넘게 걸리므로 조교가 브라우저를
띄워놓고 기다릴 수는 있지만 **탭을 닫으면 안 되는 구조는 못 씁니다.**
진행 상황이 서버에 남아야 합니다 (§13.9).

### 🔴 이 답안지가 정확히 경계선이었는데 화면이 아무 말도 안 했습니다

```
사람 기준   오답 5 / 허용 7 → PASS (여유 2)
시스템 기준 오답 7 / 허용 7 → PASS (여유 0)   ← 한 문항만 더면 FAIL
```

`compare()`가 `nearBoundary`를 계산은 하고 있었지만 **API 응답에도, 화면에도
내보내지 않고 있었습니다.** [12.8](12-page-level-grading.md)에서 "경계선 검수"를
Phase 1의 핵심 안전장치로 잡아놓고 정작 가장 필요한 장에서 침묵한 것입니다.

→ 응답에 `nearBoundary`·`margin`을 싣고, 화면에 눈에 띄게 띄웁니다.

> 🔶 **커트라인에 걸렸습니다 — 반드시 확인하십시오.**
> 오답 7개 / 허용 7개 — 여유가 없습니다. 한 문항만 더 틀렸으면 결과가 뒤집힙니다.

### 🟢 시스템이 사람보다 정확했습니다 — 그런데 그게 위험도 만듭니다

선생님이 놓친 오답 2개를 잡아냈습니다. [12.8](12-page-level-grading.md)에
적어둔 경고가 실제로 확인된 셈입니다 — **선생님 채점은 완전한 정답 라벨이
아닙니다.** 벤치마크에서 이런 건은 "우리만 오답"으로 집계돼 마치 우리가
틀린 것처럼 보이지만, 사실은 우리가 맞은 것입니다.

**동시에 이게 운영 리스크입니다.** 시스템이 습관적으로 선생님보다 2개를 더
잡는다면, **커트라인 근처 학생들이 사람 채점 때보다 더 자주 탈락합니다.**
이번 답안지가 정확히 그 경계에 있었습니다 — 사람은 여유 2개, 시스템은 여유 0개.

이건 버그가 아니라 **방침 문제**입니다.

| 선택 | 결과 |
|---|---|
| 시스템 기준대로 | 더 정확하지만 재시험이 늘어남. 학부모 문의도 늘어날 수 있음 |
| 선생님 관행에 맞춤 | 마찰이 적지만 틀린 답이 통과함 |
| **경계선만 사람이 확정** | 대부분 자동, 갈리는 지점만 선생님이 결정 ← 지금 설계 |

세 번째가 지금 설계이고, 이번 사례가 그 필요를 실증했습니다.
**"더 정확한 것"이 곧 "그대로 써도 되는 것"은 아닙니다.**

### 잘 된 것

- 학생이 쓴 것을 **매우 높은 확률로 바르게 전사**
- 촬영된 범위 안에서 **오답 수 판독이 정확히 일치**
- **밀림 없음**
- 이미 빨간펜으로 채점된 시험지였는데도 전사가 견뎠습니다 —
  [08](08-sample-analysis.md)에서 걱정하던 것보다 나았습니다

---

## 13.5 남은 위험

| 위험 | 상태 | 대응 |
|---|---|---|
| 전사 고쳐 읽기 | 🔴 미해결 ([12 §12.13](12-page-level-grading.md)) | 전 장 검수가 M1~M2를 덮음. `probe_reading`으로 별도 추적 |
| 확신도가 고쳐 읽기를 못 잡음 | 🔴 미해결 | M3의 라우팅을 확신도**만으로** 설계하지 말 것 |
| 자유 나열형 시험지 | 🟡 대상 밖 | M1은 단어시험부터. 유형 판단은 데이터가 쌓인 뒤 |
| 개인정보 동의 | 🟡 진행 필요 | **기술과 무관하게 리드타임이 있음.** 지금 시작 |

**가장 큰 위험이 "전 장 검수"로 덮인다는 점이 중요합니다.**
고쳐 읽기가 남아 있어도 사람이 전부 확인하는 동안에는 오채점이 나가지 않습니다.
그 사이에 데이터가 쌓이고, 그 데이터로 검수를 줄일지 판단합니다.

---

## 13.7 사진 보관 — 90일

실제 업로드 샘플 14장의 평균이 **1.53MB**(0.55~2.73)였습니다. 하루 300장이면:

| 무엇을 저장하나 | 월 증가 | 90일 보관 시 정상 상태 |
|---|---:|---:|
| 휴대폰 원본 그대로 | 9.9GB | 29.7GB |
| **API에 보낸 축소본(2576px)** | **2.6GB** | **7.7GB** |

**축소본을 저장합니다.** 용량 때문만이 아니라 **그게 감사에 맞는 물건**이기 때문입니다.
나중에 "왜 이렇게 채점했나"를 되짚을 때 봐야 할 것은 휴대폰 원본이 아니라
**모델이 실제로 본 이미지**입니다.

보관 기간은 **90일**로 둡니다. 재시험·이의 제기 주기를 덮으면서, 학생 답안
사진을 무기한 쌓아두지 않습니다. 채점 결과(전사·판정·검수 기록)는 이미지와 분리해
계속 보관합니다 — 개인정보 부담이 훨씬 작고 정확도 추적에 필요합니다.

> 저장소 걱정이 없어도 **개인정보는 안 쌓는 게 원칙**입니다.
> 유출 사고가 나도 잃을 게 없는 상태를 유지합니다.

---

## 13.6 시작 지점

M1의 첫 커밋은 **Next.js 프로젝트 뼈대 + `grade_page.py`의 프롬프트·스키마를
TypeScript로 옮기는 것**입니다. 파이썬 도구가 이미 실측으로 검증됐으므로
옮기는 작업은 재설계가 아니라 이식입니다.

그다음 순서:

1. 사진 한 장을 올려 채점 결과 JSON을 화면에 찍기 (배관 확인)
2. 여러 장 + 큐 + 진행 표시
3. 검수 화면
4. 결과·명단 화면
5. 실제 수업에서 한 반 돌려보기 ← **여기가 M1 완료**

---


## 13.9 🔴 접수 단위로 다시 짰습니다 (2026-08-08)

**설계를 하나 잘못 깔았습니다.** "한 반이 같은 시험을 본다"고 놓고
`exams` 표를 만들었는데, 원장님 지적으로 뒤집혔습니다.

> 보통 보는 시험이 학생별로도 다 달라서 굳이 한 반을 다 채점하는 방식으로
> 할 필요는 없어.

같은 반이라도 학생마다 진도가 달라 **보는 시험이 다릅니다.** 그러면
`exams`는 묶을 것이 없는 빈 껍데기이고, 시험을 만들라고 요구하는 화면은
**조교에게 없어도 될 일을 시키는 것**입니다.

### 실제 흐름

```
조교A: 학생1 접수 → 즉시 채점 시작
              ↓ (채점 도는 중)
       학생2 접수 → 이것도 즉시 채점 시작
조교B: 학생3 접수 → 마찬가지. 조교A와 무관하게

한 학생의 시험지가 두 조교에게 동시에 접수될 일은 없음
```

**"모아서 돌린다"가 아니라 "받는 대로 흘려보낸다"입니다.**
조교는 화면을 하루 종일 열어두고 학생이 낼 때마다 찍습니다.

### 무엇이 없어졌나

| 없앤 것 | 왜 |
|---|---|
| `exams` 표 | 묶을 것이 없음. 시험 이름은 머리말에서 읽어 답안지 행에 둠 |
| 시험 만들기 화면 | 조교가 채울 항목이 하나 더 있는 것뿐 |
| 사진을 학생별로 자르는 UI | 애초에 한 번에 한 학생분만 찍음 |
| `claim_sheets(exam, n)` | 시험 단위로 집을 이유가 사라짐 |

### 무엇이 남았나

시험 단위로 두려던 **커트라인·철자 방침**은 갈 곳이 달라졌습니다.

| 항목 | 어디로 |
|---|---|
| 커트라인 | **시험지에서 읽습니다.** 못 읽었을 때만 그 답안지에 직접 넣습니다 |
| 철자 엄격/관대 | 조교 화면에 남는 값. 한 번 정하면 다음 학생에도 그대로 |
| 반 | 선택 사항. 적어두면 명단이 반별로 묶입니다 |

**커트라인을 미리 받지 않는 것이 중요합니다.** 학생마다 시험이 다른데
앞 학생의 커트라인이 다음 학생에게 조용히 붙으면 판정이 틀립니다.

### 커트라인은 다시 채점하지 않고 고칩니다

1차 현장 테스트에서 빨간펜이 머리말을 덮어 커트라인을 못 읽었습니다.
그때 모자란 것은 **숫자 하나**뿐인데 다시 채점하면 $0.14가 또 나갑니다.

→ 전사도 판정도 이미 저장돼 있으므로 `/api/sheets/{id}/cutline`이
**모델을 안 부르고 셈만 다시 합니다.** 답안지 줄에 입력칸이 그 자리에 뜹니다.

### 큐 — 집는 방법이 둘

```
claim_sheet(id)   방금 접수한 그 장을 집는다      ← 접수 직후
claim_next(n)     떠도는 것을 아무거나 집는다      ← 화면을 연 사람이 쓸어담음
```

뒤쪽이 있어야 **조교가 채점 도중 창을 닫아도 그 한 장이 버려지지 않습니다.**
조교B의 브라우저가 조교A가 놓고 간 것을 주워 마저 채점합니다 — 채점은
누가 돌리든 결과가 같으므로 나눌 이유가 없습니다.

둘 다 `for update skip locked`라 동시에 눌러도 겹치지 않습니다.
브라우저 하나가 동시에 도는 갈래는 넷까지입니다.

### 화면

| 경로 | 무엇 |
|---|---|
| `/` | **접수** — 사진 찍어 접수, 그날 접수 목록이 실시간으로 갱신 |
| `/quick` | 저장 없이 한 명분만 눌러보는 자리 |

목록 위에 나오는 숫자 중 중요한 것은 "몇 명 끝났나"가 아닙니다.

> 🔶 **N명은 사람이 반드시 봐야 합니다** — 커트라인·밀림·판정 보류

### 데이터 구조에서 유지된 것

시험 표만 빠졌고 나머지 판단은 그대로입니다.

- **큐 테이블을 따로 두지 않습니다.** 일감의 단위가 답안지 한 벌이고
  그게 곧 `sheets`의 한 행입니다. 두 곳에 적으면 언젠가 어긋납니다.
- **문항은 표로 둡니다.** `items.teacher_correct`(선생님이 뒤집은 것만),
  `final_correct`(화면이 보는 값), `overturned`(정확도 측정) — 뒤의 둘은
  DB가 계산합니다.
- **RLS는 "직원인가"까지만.** 역할로 갈리는 것은 확정 하나이고 트리거가 막습니다.
- **사진은 축소본만 90일.** 지운 뒤에도 행은 남깁니다.

### 다음 — 3단계 검수 화면

지금은 답안지 한 줄에 오답 수와 PASS/FAIL만 나옵니다. 문항을 열어 고치고
확정하는 것이 3단계이고, 그 고친 기록이 `items.teacher_correct`에 쌓여
정확도 데이터가 됩니다.

---

## 13.10 3단계 — 검수 화면 (2026-08-08)

접수 목록에서 답안지를 누르면 `/sheets/{id}`가 열립니다.
[13.2](#132-무엇을-만들지--조교의-하루로-정의합니다)의 **"선생님이 채점한다 →
선생님이 확인한다"가 실제로 일어나는 화면**입니다.

### 무엇을 보여주나

| | 왜 |
|---|---|
| 모델이 본 사진 (서명 URL) | **전사가 종이와 같은지**는 사진을 봐야 압니다 |
| 문항별 제시어·학생이 쓴 것·정답·판정·비고 | 채점의 근거 전부 |
| 경계선·밀림·일부만 찍힘 경보 | 여기가 사람을 부른 이유 |
| 커트라인 입력칸 (못 읽었을 때) | 다시 채점하지 않고 셈만 고침 |

### 고치면 그 자리에서 다시 셉니다

문항의 ○/✗를 누르면 `items.teacher_correct`에 들어가고, 곧바로
`/api/sheets/{id}/recount`가 **모델 없이** 오답 수와 PASS/FAIL을 다시 셉니다.

```
선생님이 1번을 정답 처리 → 오답 7 → 6 → 여유 0 → -1 → 경계선에서 벗어남
```

**셈은 서버가 합니다.** 화면이 따로 세면 언젠가 서버와 어긋나고,
그 어긋남이 학생의 PASS/FAIL입니다.

다시 채점(모델 재호출)을 하지 않는 이유는 비용($0.14)만이 아닙니다.
**새로 전사하면 선생님이 방금 검수한 내용이 덮입니다.**

### 시스템과 같은 값이라도 누르면 기록합니다

`teacher_correct`는 "선생님이 바꾼 것만" 채우는 칸으로 설계했지만,
**같은 값을 눌러도 채웁니다.**

```
teacher_correct is null      아무도 안 봤다
teacher_correct = correct    사람이 보고 그대로 뒀다   ← 이게 필요한 사실입니다
teacher_correct ≠ correct    사람이 뒤집었다 (overturned)
```

검수를 줄일지 판단하려면 **"봤는데 맞더라"가 몇 건인지**를 알아야 합니다.
안 본 것과 뭉뚱그리면 분모가 사라집니다.

### 확정은 선생님만, 취소도 있습니다

조교 화면에서는 확정 버튼이 안 보이고, 보이더라도 DB 트리거가 막습니다.
잘못 눌렀을 때 나갈 길로 **확정 취소**를 뒀습니다 —
취소하면 "누가 언제 확정했다"는 도장도 함께 지웁니다(0005).
남겨두면 확정되지 않은 답안지에 확정 기록이 붙어 나중에 잘못 읽힙니다.

### 다음 — 4단계 결과·명단 화면

`retest_roster`·`wrong_items` 뷰는 이미 있습니다. 남은 것은 화면과
내보내기(복사·인쇄)입니다.

---

## 13.11 4단계 — 결과·명단 화면 (2026-08-08)

`/roster`. 조교가 하던 **"오답 정리 → 재시험 명단 만들기"**가 이 화면입니다.

| 칸 | 무엇 |
|---|---|
| 재시험 | **확정된 FAIL만.** 반별로 묶임 |
| 통과 | 확정된 PASS |
| 오답 목록 | 확정된 답안지의 오답을 학생별로 |

각 칸에 **복사** 버튼, 화면 위에 **인쇄** 버튼이 있습니다.
인쇄하면 조작 요소는 빠지고 명단만 나갑니다.

### 확정 안 된 것을 어느 칸에도 넣지 않습니다

이게 이 화면의 유일한 어려운 판단입니다.

```
채점 중 · 검수 대기 · 실패  →  전부 "미확정"
```

통과로 세면 **재시험 갈 학생이 집에 갑니다.** 재시험으로 세면 **통과한
학생을 붙잡습니다.** 둘 다 안 되므로 세 번째 칸을 만들고, 남아 있으면
화면 맨 위에 띄웁니다.

> ⏳ **아직 확정되지 않은 답안지가 3장 있습니다. 지금 명단은 완성이 아닙니다.**

**복사한 글 안에도 같은 문구가 들어갑니다.** 화면 경고는 붙여넣기를 따라가지
않기 때문입니다 — 메신저에 붙인 명단만 보고 학생을 보내면 경고를 본 사람이
없습니다.

### 검수 전 판정으로 오답노트를 뿌리지 않습니다

`wrong_items` 뷰는 확정 여부를 안 봅니다(그래야 "덜 됐다"를 셀 수 있습니다).
거르는 것은 화면이 합니다 — 명단에 나가는 오답은 **확정된 답안지의 것뿐**입니다.

### 오답은 제시어 → 쓴 것 → 정답으로 적습니다

```
7. expectation → 관전 (정답: 관점)
12. refrigerator → (무응답) (정답: 냉장고)
```

무응답도 적습니다. 빈칸으로 두면 **왜 틀렸는지가 안 보입니다.**
이 형식 그대로 오답노트·재시험지의 재료가 됩니다(M4).

### M1이 끝났습니다

```
✅ 접수 — 사진 찍어 올리면 즉시 채점
✅ 큐 · 진행 표시 — 창을 닫아도 안 잃음
✅ 검수 화면 — 문항별로 고치고 확정
✅ 결과·명단 화면 — 재시험 명단 · 오답 목록 · 복사 · 인쇄
⬜ 실제 수업에서 한 반 돌려보기  ← 남은 것
```

마지막 항목은 코드가 아니라 **개인정보 동의서**가 막고 있습니다([13.5](#135-남은-위험)).

---

## 13.12 모델 비교 — 값싼 모델로 바꾸면 무엇을 잃는가 (2026-08-08)

`/bench` (관리자만). 하루 300쪽이면 월 120만 원이라 이건 실제 돈 문제입니다.

### 무엇을 재느냐가 답을 바꿉니다

문항 정답률로 재면 **"97%면 충분한가?"라는 답 없는 논쟁**이 됩니다.
이 시스템이 내놓는 것은 PASS/FAIL 하나이고 학생에게 일어나는 일도 그것뿐이므로,
첫 지표는 **판정이 갈리는가**입니다([12 §12.8](12-page-level-grading.md)).

```
판정 일치 96.0% (24/25장)   ← 결론
뒤집힘 1장                  ← 이게 있으면 못 바꿉니다
한쪽만 판정 2장             ← 값싼 모델이 커트라인을 못 읽은 것
```

나머지 숫자(전사 차이·놓친 칸·문항 정오)는 **갈린 이유를 짚기 위한** 것이지
그 자체가 합격선이 아닙니다.

### 한쪽만 판정한 장을 일치율에 넣지 않습니다

값싼 모델이 머리말을 못 읽어 커트라인이 없으면 판정이 안 나옵니다.
그걸 "불일치"로 세면 지나치게 나빠 보이고, **"일치"로 세면 일치율이 부풀려집니다.**
셋째 칸으로 뺍니다 — 명단 화면의 미확정과 같은 발상입니다(§13.11).

### 실제 결과를 건드리지 않습니다

`model_trials` 표에 따로 쌓습니다. `sheets`·`items`는 **학생에게 나간 결과**이고
여기는 "만약 다른 모델이었다면"입니다. 섞으면 실험 때문에 판정이 바뀝니다.

커트라인·철자 방침은 **실제 채점과 같은 규칙**을 씁니다. 조건을 바꿔놓고
비교하면 갈린 이유가 모델 차이인지 조건 차이인지 알 수 없습니다.

### 기준은 선생님이 고친 값이 아닙니다

`items.correct`(시스템이 내놓은 것)를 기준으로 씁니다. 여기서 답하려는 질문은
**"값싼 모델이 지금 나가는 결과를 재현하는가"**이고, 선생님 판정을 기준에 두면
모델 차이와 선생님의 수정이 뒤섞입니다.

### 실패도 결과로 남깁니다

값싼 모델이 스키마를 못 맞추거나 거절하면 그 사유를 `error`에 적어 화면에
띄웁니다. 지우면 **"돌려봤더니 잘 되더라"는 잘못된 인상**만 남습니다.

### 사고 강도도 '하위 단계'입니다

모델을 낮추는 것 말고 **같은 모델의 effort를 낮추는** 길도 있습니다.
Opus 5 + low가 Sonnet 5 + high보다 나을 수도 있고, 그건 돌려봐야 압니다.
그래서 모델과 사고 강도를 따로 고르게 두었습니다.

| 모델 | 입력/출력 (1M당) | Opus 대비 |
|---|---|---|
| Opus 5 (기준) | $5 / $25 | — |
| Sonnet 5 | $3 / $15 | 약 60% |
| Haiku 4.5 | $1 / $5 | 약 20% |

**25장을 돌리면 실험비가 $1 안팎**입니다. 월 120만 원을 걸고 판단하는 값으로는 쌉니다.

---

## 13.13 🔴 잡음 바닥을 먼저 재야 했습니다 (2026-08-08)

Sonnet 5를 재고 "뒤집힘 1장이니 못 쓴다"고 결론지었습니다.
그다음 **같은 모델(Opus 5 · high)로 같은 답안지를 다시 돌렸더니 똑같이
1장이 뒤집혔습니다.** 같은 장이었습니다.

```
박준희 · 48문항 · 커트라인 -4
  1차 Opus  오답 4 → PASS   (여유 0 · 🔶 경계선)
  2차 Opus  오답 5 → FAIL   ← 31번 하나가 갈렸을 뿐
```

**모델을 바꾸지 않았는데 판정이 뒤집혔습니다.**
이 장의 뒤집힘은 Sonnet의 흠이 아니었습니다. 잘못 귀속한 것입니다.

### 두 번 돌린 값이 잡음 바닥입니다

| | Opus vs Opus (잡음) | Sonnet vs Opus |
|---|--:|--:|
| 문항 정오 불일치 | **3** / 203 | **10** / 200 |
| 전사가 다른 칸 | **9** | **22** |
| 비용 | 103.7% | 70.4% |
| 시간 | 357초 | 364초 |

Sonnet의 차이는 잡음의 **2~3배**입니다. 여전히 나쁘지만
"뒤집힘 1장"이라는 사형선고와는 다른 이야기입니다.

**바닥을 안 재고 대상을 재면, 바닥이 대상의 탓으로 계산됩니다.**

### 경계선에서 갈린 것을 따로 셉니다

여유 0인 답안지는 **문항 하나만 흔들려도 뒤집힙니다.** 모델이 무엇이든
그렇습니다. 그래서 뒤집힘을 둘로 나눕니다.

```
뒤집힘 N장            ← 여유가 있었는데도 갈림. 이게 늘면 못 바꿉니다
경계선에서 갈림 N장    ← 원래 사람이 확정하는 자리. 모델 탓이 아닙니다
```

### 🟢 경계선 검수가 다시 한번 옳았습니다

[12 §12.8](12-page-level-grading.md)에서 커트라인 ±2를 사람에게 넘기기로
한 것은 "모델이 틀릴 수 있으니까"였습니다. 이번 실측은 더 강한 근거를 줍니다.

> **같은 모델이 같은 사진을 두 번 읽어도 그 자리에서는 답이 달라집니다.**

모델을 아무리 좋게 만들어도 없어지지 않는 성질입니다. 경계선 답안지를
사람이 확정하는 것은 임시 방편이 아니라 **영구적으로 옳은 설계**입니다.

### 사고 강도는 아직 안 재봤습니다

이번 2차는 `high`였으므로 **effort를 낮춘 결과가 아닙니다.**
`Opus 5 + low`는 여전히 미측정입니다.

### 앞으로 이 순서로 잽니다

```
1. Opus + high 를 한 번 더 돌려 잡음 바닥을 잡는다
2. 대상 조합을 돌린다
3. 대상의 차이에서 바닥을 빼고 본다
4. 뒤집힘은 '여유 있었는데도 갈린 것'만 센다
```

---

## 13.14 🔴 판정 일치율의 맹점 — 관대한 모델은 만점을 받습니다 (2026-08-08)

`Sonnet 5 + low`를 돌렸더니 **판정 일치 100%**가 나왔습니다.
비용 47%, 시간 53%. 숫자만 보면 바꿔야 합니다.

**바꾸면 안 됩니다.**

### 오답을 절반만 잡았습니다

| 학생 | Opus 오답 | Sonnet-low 오답 | 갈린 문항 |
|---|--:|--:|---|
| 기천용 | 2 | **0** | −44, −49 |
| 신균수 | 1 | **0** | −13 |
| (12칸) | 2 | **0** | −4, −8 |
| 신준상 | 3 | **2** | −24 |
| 박준희 | 4 | 4 | 없음 |

**정오가 갈린 7건 중 6건이 한 방향**입니다 — 전부 "Opus는 틀렸다는데
Sonnet-low는 맞다고 함". 잡음이면 위아래로 흩어져야 합니다.

### 왜 판정 일치율이 이걸 못 잡았나

**통과할 학생을 더 통과시켜도 판정은 그대로이기 때문입니다.**

```
오답 2 → PASS
오답 0 → PASS      판정 일치 ✅
```

극단적으로 **"전 문항 정답"이라고만 답하는 모델**도 이 표본에서는
판정 일치 100%가 나옵니다. 비교된 4장이 전부 PASS였기 때문입니다.

> 제가 이 벤치마크를 판정 일치율 하나로 설계했고, 그 지표가 여기서 뚫렸습니다.

### 고친 것

| 추가 | 무엇 |
|---|---|
| `itemsWrongOnlyBase` / `itemsWrongOnlyTrial` | 정오 불일치의 **방향** |
| `bias()` | 3건 이상이고 한쪽이 75% 이상이면 `lenient` / `strict` |
| `allComparedPass` | 비교된 장이 전부 PASS면 **표본에 판별력이 없다**고 경고 |

화면에 이렇게 뜹니다.

> 🔴 **대상이 오답을 놓치는 쪽으로 치우쳤습니다.** (놓친 오답 6 · 더 잡은 오답 1)
> **판정 일치율은 이걸 못 잡습니다** — 통과할 학생을 더 통과시켜도 판정은 그대로이기 때문입니다.
> 게다가 비교된 장이 전부 PASS라, 이 표본은 관대한 모델을 걸러낼 힘이 없습니다.

### 사고 강도가 채점 방침을 바꿉니다

같은 Sonnet 5인데 강도만 다릅니다.

| | 오답 총계(비교 가능 장) | 방향 |
|---|--:|---|
| Opus 5 · high (기준) | 11 | — |
| Sonnet 5 · **high** | 16 | 엄격 |
| Sonnet 5 · **low** | 6 | **관대** |

**`effort`는 비용 손잡이가 아니라 채점 방침 손잡이입니다.**
낮추면 판정 단계가 덜 따지고 "비슷하면 정답"으로 기웁니다.
전사 차이는 22 → 23으로 그대로였습니다 — **읽기는 안 변하고 판정만 변했습니다.**
[12 §12.7](12-page-level-grading.md)의 실측(전사 단계는 사고를 안 쓴다)과 맞습니다.

### 지금까지의 정리

| 조합 | 판정 | 전사 차이 | 정오 방향 | 비용 | 시간 | 판단 |
|---|---|--:|---|--:|--:|---|
| Opus · high (재실행) | — | 9 | 균형 | 104% | 104% | **잡음 바닥** |
| Sonnet · high | 경계선 1 | 22 | 엄격 | 70% | 106% | ❌ |
| Sonnet · low | 일치 | 23 | **관대** | 47% | 53% | ❌ |

**Sonnet은 어느 강도에서도 읽기가 2.5배 흔들립니다**(9 → 22~23).
강도는 그 위에서 채점을 엄격/관대로 흔들 뿐입니다.

### 다음에 재볼 것

`Opus 5 + low`는 여전히 미측정입니다. 이번 결과로 예측이 생겼습니다.

> 전사 차이는 잡음 바닥(9)에 가깝고, 정오는 관대 쪽으로 쏠릴 것이다.

맞으면 **"읽기는 Opus, 판정은 강도 높게"** 가 유일하게 안전한 조합이라는
뜻이고, 그건 지금 쓰는 설정입니다. 절감할 곳이 없다는 결론이 됩니다.
틀리면 그때 다시 봅니다.

**표본도 바꿔야 합니다.** 지금 4장이 전부 PASS라 판별력이 없습니다.
**FAIL이 섞인 날**로 돌려야 관대한 모델이 걸립니다.

---

## 13.15 🟢 `Opus 5 + low` — 잡음 안에 들어옵니다 (2026-08-08)

| | 잡음 바닥<br>(Opus·high 재실행) | **Opus · low** | Sonnet · high | Sonnet · low |
|---|--:|--:|--:|--:|
| 여유 있는데 뒤집힘 | 0 | **0** | 0 | 0 |
| 문항 정오 불일치 | 3 / 203 | **2 / 214** | 10 / 200 | 7 / 200 |
| 전사가 다른 칸 | 9 | **10** | 22 | 23 |
| 놓친 칸 | 14 | **3** | 17 | 17 |
| 정오 방향 | 균형 | 균형(2건) | 엄격 | **관대** |
| 비용 | 104% | **82%** | 70% | 47% |
| 시간 | 104% | **74%** | 106% | 53% |

**`Opus·low`가 `Opus·high`와 다른 정도가, `Opus·high`가 자기 자신과 다른 정도보다
작습니다.** 잡음과 구별이 안 된다는 뜻이고, 이보다 나은 결과는 나올 수 없습니다.

### 예측은 반만 맞았습니다

> 전사 차이는 잡음 바닥(9) 근처, 정오는 **관대 쪽으로 쏠릴 것**

전사는 맞았습니다(10). **정오는 틀렸습니다** — 불일치가 2건뿐이고 둘 다
`+`(대상이 더 잡음)였습니다. `bias()`가 3건 미만이라 방향을 말하지 않았고,
그게 맞는 처리입니다.

Sonnet이 `low`에서 관대해진 것은 **강도 탓이 아니라 모델 탓**이었습니다.
Opus는 덜 따져도 판정이 흔들리지 않습니다.

### 강도는 판정 단계만 건드립니다

전사 차이가 세 조합 내내 그 모델의 값 그대로입니다.

```
Opus   high 9 → low 10     (모델이 같으면 강도를 바꿔도 읽기는 그대로)
Sonnet high 22 → low 23
```

[12 §12.7](12-page-level-grading.md)의 실측(전사 단계는 사고를 안 쓴다)이
세 번 더 확인됐습니다. 그래서 **강도를 낮춰 아낄 수 있는 것은 판정 몫뿐**이고,
그게 18%입니다.

### 🔶 경계선 장이 또 갈렸습니다

```
(10~12칸 답안지) 커트라인 -2
  Opus·high  오답 2 → PASS  (여유 0)
  Opus·low   오답 3 → FAIL
```

박준희와 다른 장인데 증상이 같습니다. **여유 0인 답안지는 무엇을 바꾸든
갈립니다.** 이제 두 번 확인됐습니다.

### ⚠️ 번호 없는 시험지에서는 정오 차이를 믿지 마십시오

17칸짜리 장의 정오 차이가 `+11`로 크게 나왔습니다. 이 답안지는
**번호가 인쇄돼 있지 않아 모델이 번호를 붙이는** 유형입니다(§13.8).

> 같은 "7번"이 **같은 칸이 아닐 수 있습니다.**

번호로 짝지어 비교하는 방식이 이 유형에서는 어긋납니다. 이 장은 마침
기준도 판정이 없어 통계에서 빠졌지만, **번호 없는 시험지가 표본에 많으면
정오 차이가 부풀려집니다.** 판정 일치만 보십시오.

### 결론과 남은 조건

**바꿔도 될 후보는 `Opus 5 + low` 하나입니다.** 다만 아직 근거가 얇습니다.

| 확인된 것 | 아직 아닌 것 |
|---|---|
| 6장에서 잡음과 구별 안 됨 | 표본이 **6장**뿐 |
| 읽기가 안 나빠짐 | 비교 가능한 4장이 **전부 PASS** |
| 비용 −18% · 시간 −26% | FAIL 답안지에서 안 재봄 |

**관대 편향은 PASS만 있는 표본에서 안 잡힙니다**(§13.14). 지금 판정이
잘 맞은 것도 그 한계 안의 이야기입니다.

→ 실사용으로 답안지가 쌓이면 **FAIL이 섞인 20~30장**으로 한 번 더 재고
그때 바꿉니다. 하루 300쪽 기준 월 22만 원 아끼는 결정이라, 몇 주 기다릴
값어치가 있습니다.

### 바꿨습니다 — 되돌릴 수 있게

운영 기본값을 **`claude-opus-5` · `low`** 로 옮겼습니다. 다만 표본이 6장뿐이라
**코드가 아니라 환경 변수로** 뺐습니다.

```
GRADING_MODEL=claude-opus-5     비우면 claude-opus-5
GRADING_EFFORT=high             비우면 low  ← 되돌릴 때 이 줄만 넣습니다
```

Vercel 환경 변수를 바꾸고 재배포하면 끝입니다. 배포 이력을 되짚거나
커밋을 되돌릴 일이 없습니다. 모르는 값이 들어오면 **조용히 기본값으로
넘어가지 않고 오류를 냅니다** — 오타 하나로 엉뚱한 모델에 돈이 나가는 것보다
채점이 멈추는 편이 낫습니다.

**어떤 설정으로 채점됐는지는 `sheets.token_usage`에 같이 저장됩니다**(`effort`).
나중에 "언제부터 이상해졌나"를 답안지 단위로 되짚을 수 있습니다.

### 되돌릴 신호

| 보이면 | 뜻 |
|---|---|
| 검수에서 **선생님이 뒤집는 문항이 늘어남**(`items.overturned`) | 판정이 나빠짐 |
| **커트라인 근처 답안지가 늘어남** | 오답 개수가 흔들림 |
| 전사가 종이와 다른 게 눈에 띄게 늘어남 | 읽기가 나빠짐 (예상 밖) |

특히 첫 줄이 직접적인 지표입니다. 전 장 검수 중이라 **오채점이 학생에게
나가기 전에 잡힙니다** — 지금 바꿔도 안전한 이유가 그것입니다.

---

## 13.16 사진 90일 자동 삭제 (2026-08-08)

[14 §14.7](14-consent.md)에서 **동의서에 적을 약속인데 지우는 코드가 없다**고
잡아둔 것을 붙였습니다. "90일 후 자동 파기"라고 적어놓고 안 지우는 것이
가장 나쁩니다.

```
매일 새벽 3시(KST) — Vercel Cron → GET /api/retention
   ↓
expired_pages 뷰에서 100장씩 집어
   ↓
스토리지 파일 삭제 → sheet_pages.purged_at 찍기
```

### 파일을 먼저 지우고 도장은 나중에 찍습니다

순서를 거꾸로 하면 **"지웠다"고 적어놓고 파일이 남는 일**이 생깁니다.
그건 학부모에게 한 약속을 어기는 것입니다.

반대로 파일만 지우고 도장을 못 찍으면 다음 실행이 **없는 파일을 한 번 더
지우려 할 뿐**이고, 그건 아무 해가 없습니다. 실패하는 방향을 고른 것입니다.

### 기간은 SQL 한 곳에만 있습니다

`expired_pages` 뷰가 `90 days`를 정의하고, 앱은 그 뷰를 읽기만 합니다.
환경 변수로 빼지 않았습니다 — **이 숫자는 동의서에 적힌 약속**이라
바꾸려면 동의서도 같이 바꿔야 하고, 그건 마이그레이션만큼 무거운 일이
맞습니다. 두 곳에 적으면 언젠가 어긋납니다.

### 부를 수 있는 사람

| 누가 | 무엇으로 |
|---|---|
| Vercel Cron | `Authorization: Bearer $CRON_SECRET` |
| 관리자 | 로그인 토큰 (`/admin`의 버튼) |

둘 다 아니면 **404**를 냅니다. "권한 없음"이 아니라 없는 척합니다 —
이 주소가 있다는 것 자체를 밖에 알릴 이유가 없습니다.

### `/admin` — 약속이 지켜지는지 보는 곳

자동으로 도는 것만으로는 **정말 돌고 있는지 알 수 없습니다.**

```
갖고 있는 사진 · 지워야 할 사진 · 지운 기록 · 가장 오래된 사진 날짜
```

`지워야 할 사진`이 0이 아니면 자동 실행이 안 돌았거나 실패한 것이고,
화면이 노랗게 알려줍니다. 손으로 돌리는 버튼도 있습니다.

### 남은 것 — 동의 철회

[14 §14.7](14-consent.md)의 두 번째 항목(**학생 단위 즉시 삭제**)은
아직입니다. `deleteSheet`는 답안지 단위라 "이 학생 것 전부"를 못 지웁니다.
동의서를 돌리기 전에 이것도 필요합니다.

---

## 13.17 직원 관리와 사용 기록 (2026-08-08)

목적은 하나입니다 — **근무 시간 외 사적 사용을 막는 것.**
답안지 한 장에 $0.14가 나가고 그 돈은 학원이 냅니다.

### 🔴 만들다가 구멍을 찾았습니다

`/api/grade`(빠른 시험)에 **인증이 아예 없었습니다.**

```
주소만 알면 누구나 학원 API 예산을 씁니다
답안지 행이 안 생기므로 기록도 안 남습니다
```

사적 사용을 막으려는 마당에 **가장 큰 통로가 열려 있었습니다.**
로그인·직원 확인을 붙이고, `/quick` 화면도 로그인 뒤에만 열리게 했습니다.

### `usage_events` — `sheets`와 다른 사실입니다

```
sheets        무엇을 채점했는가 (일의 결과)
usage_events  누가 언제 돈을 썼는가 (지출의 기록)
```

겹쳐 보이지만 다릅니다. 재시도로 한 답안지를 두 번 채점하면 `sheets`는
한 행이고 여기는 두 행입니다. 모델 비교는 `sheets`를 안 건드리지만 여기 남습니다.
**그리고 빠른 시험은 답안지 행이 아예 없어 여기가 유일한 기록입니다.**

고치거나 지우는 정책을 두지 않았습니다 — **지출 기록은 고쳐지면 안 됩니다.**

### 볼 수 있는 사람

| | 자기 기록 | 남의 기록 |
|---|:--:|:--:|
| 조교·선생님 | ✅ | ❌ |
| 관리자 | ✅ | ✅ |

**남길 때도 자기 이름으로만** 남길 수 있습니다(RLS `staff_id = auth.uid()`).
남의 이름으로 기록을 만들 수 없습니다.

### 근무 시간은 학원이 정합니다

무엇이 '밖'인지는 시스템이 정할 수 없습니다. `settings`에 요일과 시각을 두고
관리자가 화면에서 바꿉니다. 기본은 **월~토 13:00~23:00 (한국 시간)**.

시각 계산은 **UTC+9 고정**으로 합니다. 브라우저 시간대에 기대면 관리자가
여행 중일 때 조용히 틀립니다. 한국은 서머타임이 없어 고정이 정확합니다.

### 합계가 아니라 분포를 봅니다

```
호출 312 · 근무 시간 외 4      ← 합계
시간대별 막대 (0~23시)          ← 새벽 두 시가 튀는 게 여기서 보입니다
```

합계만 보면 "밤에 썼다"가 안 보입니다. 근무 시간 밖 막대는 노랗게 칠하고,
해당 기록 20건을 이름·종류·비용과 함께 그대로 나열합니다.

**`빠른 시험` 열을 따로 뒀습니다.** 답안지를 안 남기는 호출이라
사적 사용이 새어나간다면 그 열이 먼저 움직입니다.

### 퇴사자는 지우지 않고 끕니다

`staff.active = false`. 행을 지우면 **그 사람이 채점한 기록이 주인을 잃고**
"누가 받았나"를 영영 못 답합니다. 끄면 `is_staff()`가 거짓이 되어 로그인해도
아무것도 못 봅니다.

관리자는 **자기 자신을 못 끕니다.** 스스로를 잠그면 되돌릴 길이 SQL뿐입니다.

### ⚠️ 직원에게 알리십시오

사용 기록은 **직원의 개인정보**이기도 합니다. 근로계약서나 사내 규정에
"업무용 계정의 사용 기록을 확인한다"를 명시하고 알리는 것이 안전합니다.
기술적으로는 이미 되지만, **알리지 않고 보는 것과 알리고 보는 것은 다릅니다.**
[14](14-consent.md)의 변호사 확인 목록에 이 항목도 넣으십시오.

---

## 13.18 반을 다음 학생에게 남기지 않습니다 (2026-08-09)

접수 화면에서 **반은 유지, 이름은 비움**으로 만들었는데 원장님이 물었습니다.

> 반이 왜 다음 학생에게 그대로 붙는지? 반도 계속 바뀔 수 있음.

**제 가정에 근거가 없었습니다.** "연달아 같은 반이 온다"고 놓고 짰는데,
학생마다 시험이 다르다는 것(§13.9)과 같은 종류의 잘못된 가정입니다.
조교는 반 순서대로가 아니라 **학생이 내는 순서대로** 받습니다.

### 유지하면 조용히 틀립니다

```
중3 A 접수 → 다음 학생은 고1인데 '중3 A'가 그대로 들어 있음
→ 조교가 못 보고 접수 → 고1 학생이 중3 A 명단에 들어감
```

화면에 아무 표시도 안 뜹니다. 커트라인을 미리 안 받기로 한 것,
이름을 안 남기기로 한 것과 **같은 이유**입니다.

### 값을 남기지 않고 후보를 내놓습니다

접수하면 반도 비웁니다. 대신 **최근에 쓴 반 여섯 개를 단추로** 내놓습니다.

| | 유지 | 최근 목록 |
|---|---|---|
| 같은 반이 이어질 때 | 0번 | **1번 누름** |
| 반이 바뀔 때 | 못 보면 **틀림** | 안 누르면 **아무 반도 안 붙음** |

한 번 누르는 값으로 조용한 오류를 없앱니다. 그리고 반이 정말 계속
바뀐다면 유지는 애초에 도움이 안 되므로, 최근 목록이 두 경우 모두에서
낫습니다.

### 기기에 남는 것

```
strict   철자 방침 — 학생마다 바뀌는 값이 아니라 학원 방침입니다 (유지)
recent   최근 쓴 반 — **값이 아니라 후보**입니다
```

반과 이름은 **값을 안 남깁니다.**

---

## 13.19 GPT도 같은 파이프라인에 태웁니다 (2026-08-09)

> 원장: "GPT로도 테스트해보고 싶어. 내가 할 일은 Open AI API만 vercel
> 환경변수에 넣으면 돼?"

키만으로는 안 됐습니다. 그때까지 코드는 **Anthropic 하나를 전제**하고
있었습니다 — `transcribe`·`judge`가 `Anthropic` 타입을 그대로 받았고,
실험 라우트의 모델 목록이 `claude-` 셋으로 고정돼 있었습니다.

### 회사가 맞춰야 할 것은 한 가지뿐이었습니다

파이프라인이 모델에게 요구하는 것은 결국 하나입니다.

```
이미지(선택) + 글 → 이 스키마에 맞는 JSON
```

그래서 `ModelClient` 하나만 인터페이스로 두고 회사별 어댑터를 붙였습니다.
`compare`·`checkDrift`·`diffRuns`·`/bench`는 **한 줄도 안 고쳤습니다.**
비교 화면이 재는 것(판정이 갈리는가, 어느 칸을 다르게 읽었는가)은 애초에
회사와 무관한 값이었기 때문입니다.

| 파일 | 하는 일 |
|---|---|
| `lib/grading/provider.ts` | 타입 + 모델 목록 + 단가. **환경 변수를 안 읽습니다** |
| `lib/grading/client.ts` | Anthropic 어댑터 · `clientFor(model)` |
| `lib/grading/openai.ts` | OpenAI 어댑터 (Responses API) |

`provider.ts`가 환경 변수를 안 읽는 이유는 **모델 목록을 화면도 쓰기**
때문입니다. 브라우저에는 서버 환경 변수가 없어서, 거기서 값을 읽으면 화면과
서버가 조용히 다른 목록을 갖게 됩니다. 이미 같은 종류로 한 번 데었습니다
(§13.9의 `NEXT_PUBLIC_` 인라인).

### 두 회사가 다른 곳

| | Anthropic | OpenAI |
|---|---|---|
| 출력 상한 | `max_tokens` 16,000 | `max_output_tokens` 32,000 |
| 사고 토큰 | 출력에 안 셈 | **출력에 포함** |
| 안 끝났을 때 | 예외 | `status: "incomplete"` — 조용히 짧은 JSON |

상한을 그대로 뒀다면 50문항 전사가 사고 도중에 잘렸을 것이고, `incomplete`를
안 봤다면 그게 "GPT가 못 한다"로 기록됐을 것입니다. 재려는 것은 모델의
실력이지 우리 설정 실수가 아닙니다.

### 🔴 실제 채점은 못 넘어가게 막았습니다

`GRADING_MODEL`은 **Anthropic 모델만** 받습니다. `gpt-`를 넣으면 앱이 아예
안 뜹니다.

동의서에 적은 국외 이전 대상이 Anthropic PBC 하나이기 때문입니다
([14 §14.8](14-consent.md)). 실제 채점이 다른 회사로 넘어가는 것은
**환경 변수 한 줄로 일어나면 안 되는 종류**의 변화입니다 — 그러려면 동의서를
고치고 동의를 다시 받아야 합니다. 회귀 테스트로 못 박아 두었습니다.

`/bench` 실험은 GPT를 고를 수 있습니다. 대신 고르는 순간 화면이 "이건
동의서 밖"이라고 말합니다. 그리고 실험 라우트를 **관리자로 막았습니다** —
그전에는 화면에서만 막혀 있어서 주소를 아는 직원은 그대로 부를 수 있었습니다.

### 모르는 단가를 0으로 적지 않습니다

`costUsd`가 이제 `number | null`입니다. 예전에는 모르는 모델을 `[0, 0]`으로
쳤는데, 그러면 GPT 실험이 전부 `$0.000`으로 찍혀 **"GPT가 훨씬 싸다"는
결론이 저절로 납니다.** 비용을 비교하려고 만든 화면에서 가장 나쁜 거짓말입니다.

처음에는 GPT 쪽 단가를 몰라 비워 두고 화면에 `—`를 띄웠습니다. 2026-08-10에
원장님이 OpenAI 공식 가격표를 확인해 주셔서 채웠습니다.

### 후보는 셋입니다 (2026-08-10)

| 모델 | 입력 | 출력 | 전사 1쪽 기준 Opus 대비 |
|---|---:|---:|---:|
| GPT-5.6 Sol | $5 | $30 | **115%** |
| GPT-5.6 Terra | $2 | $12 | **46%** |
| GPT-5.6 Luna | $0.20 | $1.20 | **5%** |
| (참고) Claude Sonnet 5 | $3 | $15 | 60% |

**2026-07-30에 Luna가 80%, Terra가 20% 내렸습니다.** 그 전에 계산한 값은
전부 틀립니다. [12 §12.2](12-page-level-grading.md)의 장당 비용 표도 그
이전 단가로 적힌 것입니다.

절대 금액이 아니라 **비율로 적었습니다.** 실제 채점 한 건은 전사 여러 쪽 +
판정 한 번이라 쪽수마다 금액이 달라지는데, 비율은 단가로만 정해지므로
쪽수와 무관하게 맞습니다. 화면의 안내 문구와 실제 계산이 갈리지 않도록
회귀 테스트로 묶어 두었습니다.

**Luna의 5%가 이 실험의 진짜 질문입니다.** 20분의 1이면 값이 문제가 아니게
됩니다 — 문제는 [§13.14](#1314--판정-일치율의-맹점--관대한-모델은-만점을-받습니다-2026-08-08)에서 Sonnet
low가 걸린 그 함정입니다. 판정 일치율 100%를 받고도 오답 절반을 놓쳤습니다.
**PASS만 있는 표본에서는 관대한 모델이 안 걸립니다.** Luna를 잴 때 봐야 할
것은 일치율이 아니라 `itemsWrongOnlyBase`와 `allComparedPass`입니다.

안 넣은 것 둘:

| | 왜 |
|---|---|
| Long Context 할증 (272K 초과 시 2배) | 우리 요청은 5천 토큰이라 걸릴 일이 없습니다 |
| Batch API 50% 할인 | 조교가 접수하고 **기다리는** 흐름이라 못 씁니다 |

---

## 13.20 채점 모델을 설정 화면으로 (2026-08-10)

> 원장: "설정화면에서 관리자가 기본으로 돌아가는 모델을 선택할 수 있게
> 해주면 좋을 것 같아. 그리고 채점하는 유저도 어떤 모델 기반으로 돌아가는지
> 알 수 있게 표시해주면 좋겠어."

### 환경 변수는 "되돌릴 수 있다"가 아니었습니다

§13.15에서 기본값을 `Opus·low`로 옮기면서 **코드가 아니라 환경 변수로**
뺐습니다. 표본이 6장뿐이라 언제든 되돌려야 했기 때문입니다.

그런데 되돌리려면 Vercel에 들어가 값을 고치고 재배포해야 했습니다.
**원장님이 직접 못 하면 되돌릴 수 있는 게 아닙니다.** 제가 없으면 못 바꾸는
것을 "안전장치"라고 부른 셈입니다.

### 그리고 화면이 말할 수가 없었습니다

이게 더 큰 문제였습니다. 환경 변수는 서버만 압니다. 그래서

- 조교는 자기가 접수한 답안지가 무엇으로 채점되는지 몰랐고
- "요즘 결과가 좀 이상한데" 같은 얘기가 나와도 짚을 데가 없었고
- `/bench`의 기준이 지금 설정과 다른데도 화면이 알 수 없었습니다(§13.19)

### `settings` 표로 옮겼습니다

```
관리 화면 → 채점 모델 → [Opus 5 ▾] [low ▾] [바꾸기]
                              ↓
                   settings.grading_model / grading_effort
                              ↓
         /api/grade-sheet · /api/grade 가 매 호출 읽음
                              ↓
   접수 화면 "채점 모델 Opus 5 · low"   답안지 화면 "그때 무엇으로 채점됐나"
```

**환경 변수는 죽었습니다.** 두 곳에 두면 화면이 한쪽을 보고 채점이 다른
쪽을 보는 날이 옵니다 — §13.19에서 방금 그 종류에 데었습니다.

### 🔴 동의서 선을 두 겹으로

실제 채점이 GPT로 넘어가면 학생 답안지가 동의서에 없는 회사로 나갑니다
([14 §14.8](14-consent.md)). 화면의 선택지에서 GPT를 빼는 것으로는 부족합니다 —
선택지는 나중에 누가 고칩니다.

| 겹 | 무엇 |
|---|---|
| 코드 | `normalizeGrading()`이 `provider === "anthropic"`만 통과 |
| **DB** | `check (grading_model like 'claude-%')` |

DB 쪽이 본체입니다. **어떤 경로로 들어와도** — 화면이든, 직접 SQL이든 —
안 들어갑니다.

### 못 읽으면 멈춥니다

마이그레이션이 밀린 채로 배포가 먼저 붙으면 이 값이 `undefined`입니다.
그때 기본값으로 메우지 않습니다.

| | 왜 |
|---|---|
| 채점 | **던집니다.** 모르는 설정으로 돈을 쓰지 않습니다 |
| 접수 화면 | 문구를 **안 띄웁니다.** 지어낸 이름이 걸리면 안 됩니다 |
| 관리 화면 | 🔴 무엇이 저장돼 있는지 그대로 보여주고 마이그레이션을 가리킵니다 |

§13.18의 `normalizeHours`와 같은 규칙입니다 — **모르는 것을 아는 척하지
않습니다.**

### 답안지 화면은 '그때' 설정을 보여줍니다

접수 화면은 **지금** 설정, 답안지 화면은 **그 장을 실제로 채점한** 설정
(`token_usage[0]`)입니다. 둘이 달라야 맞습니다. 모델을 바꾼 뒤 옛 답안지를
열고 "왜 이 판정이 나왔나"를 되짚는 자리이기 때문입니다.

### 바꾸기 전에 재라고 말합니다

관리 화면에서 값을 고르면 저장 전에 안내가 뜹니다.

> 🔶 바꾸기 전에 재보십시오. `/bench`에서 같은 답안지로 돌려보면 판정이
> 갈리는지 먼저 보입니다. 특히 **FAIL이 섞인 날**로 재야 합니다 —
> 통과한 답안지만 있으면 관대한 모델도 100%가 나옵니다(§13.14).

단추 하나로 학생에게 나가는 판정이 바뀌는 자리라, 누르기 전에 §13.14의
함정을 한 번 더 말해줍니다.

---

## 13.21 필드 이름이 출력의 58%였습니다 (2026-08-10)

> 원장: "품질을 유지하면서 비용을 더 줄일 수 있는 방법을 찾아봐."

모델을 바꾸는 길은 §13.19–§13.20에서 막혔습니다. Sonnet도 GPT-5.6 Terra도
잡음 바닥의 두 배 이상으로 흔들렸습니다. 그러면 남은 곳은 **모델이 아니라
우리가 요구한 것**입니다.

### 어디에 돈이 나가는가

비용의 **70%가 출력**입니다([12 §12.2](12-page-level-grading.md)).
그런데 문항 하나가 뱉는 JSON을 세어 보면

```
{"no":"31","prompt":"자주 일어나는","direction":"ko2en","prefix":"f",
 "written":"frequent","blank":false,"legible":true,"erased":false,"confidence":0.95}

  전체 146자  ·  그중 필드 이름 85자 (58%)
```

**필드 이름은 아무 정보도 안 나릅니다.** 50문항이면 그것만 4,000자를 다시
사는 셈입니다. 뜻은 스키마의 `description`이 이미 나르고 있고, 구조화 출력이
모양을 강제하므로 이름은 짧아도 됩니다.

먼저 안 쓰는 필드가 있는지 전부 뒤졌습니다. **없었습니다** — `prefix`는 밀림
검증, `erased`·`legible`은 검수 화면, `direction`은 판정 규칙에 각각 쓰입니다.
버릴 것은 이름뿐이었습니다.

### `confidence` 하나만 뺍니다

| | |
|---|---|
| 값 | 문항마다 18자 |
| 쓰는 곳 | **한 군데** — 양면 사진이 겹쳤을 때 어느 쪽을 남길지 |
| 신뢰도 | [12 §12.13](12-page-level-grading.md)에서 **고쳐 읽기를 못 잡는 것이 확인됨** |

모델은 고쳐 읽으면서 0.90·0.93으로 확신에 차 있었고, 정직하게 흔들린 칸에
0.75를 줬습니다. 값을 치르고 살 이유가 없습니다.

겹친 칸을 고르는 규칙은 **읽힌 쪽을 남기는 것**으로 바꿨습니다. 경계에 걸쳐
찍히면 한쪽은 칸이 잘려 빈칸·판독불가로 읽히므로, 알고 싶은 것("어느 쪽
사진에 그 칸이 온전히 들어왔나")에 오히려 더 곧게 답합니다.

### 스키마를 두 벌 적지 않습니다

```
TRANSCRIBE_SCHEMA  ──renameProps()──▶  압축판
      (한 벌)                            (이름만 다름)
```

두 벌을 손으로 적으면 언젠가 갈리고, 갈리면 **조건이 다른 것을 비교하게
됩니다.** 설명·타입·순서는 원본에서 그대로 옵니다. 받은 뒤에는 `expand()`가
원래 이름으로 되돌리므로 `transcribe`·`judge` 바깥은 한 줄도 안 바뀝니다.

판정 단계 입력의 `JSON.stringify(payload, null, 1)`에서 들여쓰기도 뺐습니다.
그 JSON은 통째로 입력 토큰입니다.

### 🔴 이것도 재고 나서 바꿉니다

모델은 **글을 쓰면서 생각하는 부분이 있습니다.** 이름이 짧아지면 읽기가
나빠질 수도 있습니다 — 안 그럴 것 같지만, 그건 §13.15에서 "강도를 낮추면
읽기가 나빠질 것"이라고 예상했다가 틀린 것과 같은 종류의 짐작입니다.

그래서 `/bench`에 **출력 형식** 축을 하나 더 뒀습니다. 모델을 재던 방식
그대로 잽니다.

| 재는 것 | 기준 |
|---|---|
| 전사가 다른 칸 | 2026-08-10 7장의 잡음 바닥 **9** |
| 문항 불일치 | 같은 날 **1 / 147** |
| 판정 뒤집힘 | 0 |

**기본값은 아직 `full`입니다.** 압축판은 실험으로만 돌아갑니다. 잡음과
구별되지 않으면 그때 옮깁니다.

### 실측 — 전사는 통과, 판정은 탈락 (2026-08-10)

같은 7장, `Opus 5 · low · 둘 다 압축`:

| | 잡음 바닥 | 압축 | |
|---|--:|--:|---|
| 전사가 다른 칸 | 9 | **4** | 🟢 오히려 낮음 |
| 문항 불일치 | 1 / 147 | **6** / 147 | 🔴 |
| 정오 방향 | 균형 | **6 : 0 관대** | 🔴 한쪽으로 전부 |
| 판정 뒤집힘 | 0 | 0 | 🟢 |
| 비용 | 98.8% | **90.0%** | 예상(75~80%)에 못 미침 |

**전사는 멀쩡했습니다.** 이름을 줄여도 옮겨 적기는 안 나빠집니다.

**손해는 판정에서 났고, 증거는 김예지입니다** — 전사가 **완전히 같은데**
정오만 4건 갈렸고(3·12·13·14) 전부 '오답을 놓치는' 쪽이었습니다.
전사 차이로는 설명이 안 됩니다.

김예진 44번도 그 목록에 있습니다. 학생이 `requestment`라고 잘못 쓴 칸이고,
[§13.19](#1319-gpt도-같은-파이프라인에-태웁니다-2026-08-09)에서 Terra가
`requirement`로 고쳐 읽었던 바로 그 문항입니다. **압축판은 전사를 제대로
해놓고 판정에서 정답 처리했습니다.**

이유는 짐작이 갑니다. `"correct": false`를 `"c": false`로 바꾸면 모델이
**무엇을 정하는 중인지 알려주는 낱말이 사라집니다.** 옮겨 적기에는 필요 없던
것이 따지는 일에는 값을 했습니다.

### 🔴 조건 밖에 둔 변경이 운영으로 샜습니다

판정 입력의 `JSON.stringify(payload, null, 1)`에서 들여쓰기를 뺀 것을
`compact` 조건 **밖에** 뒀습니다. 실제 채점의 입력까지 바뀌었고, 재보지도
않았습니다. 게다가 잡음 바닥은 들여쓰기가 있을 때 잰 값이라 **기준선까지
흔들렸습니다.**

조건 안으로 옮겼습니다. 압축판의 판정이 관대해진 원인이 짧은 이름인지
들여쓰기인지는 지금 자료로 못 가릅니다 — **한 번에 두 가지를 바꿨기
때문입니다.**

### 그래서 `items`로 나눕니다

| | 전사 | 판정 |
|---|---|---|
| `full` | 그대로 | 그대로 |
| **`items`** | **압축** | 그대로 |
| `compact` | 압축 | 압축 — 🔴 기록용 |

아낄 수 있는 쪽만 아끼고 손해 보는 쪽은 안 건드립니다. 전사가 항목 출력의
대부분(문항당 146자 대 판정 58자)이므로 절감의 상당 부분이 남습니다.

**불리언 하나로 두 단계를 같이 켠 것이 설계 실수였습니다.** 그렇게 두면
"압축은 안 된다"로 끝났을 것이고, 실제로는 절반이 멀쩡했습니다.

### 🔴 그런데 나눠서 재보니 제 진단이 틀렸습니다

`items`(전사만 압축)를 같은 7장에 돌린 결과:

| | 잡음 바닥 | 전사만 압축 | 둘 다 압축 |
|---|--:|--:|--:|
| 전사가 다른 칸 | 9 | **4** | 4 |
| 문항 불일치 | 1 / 147 | **7** / 147 | 6 / 147 |
| 정오 방향 | 균형 | 균형(5:2) | 6:0 관대 |
| 판정 뒤집힘 | 0 | **0** | 0 |
| 비용 | 98.8% | **92.3%** | 90.0% |
| 시간 | 102% | **90%** | 100% |

**판정 스키마를 원래대로 되돌렸는데 김예지의 그 4건(3·12·13·14)이 그대로
나왔습니다.** 위에서 "손해는 판정에서 났다"고 적은 것은 **틀렸습니다.**
근거로 삼은 것이 "김예지는 전사 차이가 없는데 판정만 갈렸다"였는데,
판정을 안 건드려도 같은 4건이 나오므로 그 추론이 성립하지 않습니다.

두 압축판이 **정확히 같은 4건**을 낸 것은 우연으로 보기 어렵습니다. 원인은
전사 쪽에 있거나, 아니면 판정 단계가 원래 이 답안지에서 흔들리는 것입니다.
**지금 자료로는 못 가릅니다** — 가리려면 압축판을 한 번 더 돌려 그 4건이
재현되는지 봐야 합니다.

### 접습니다

| | |
|---|---|
| 얻는 것 | **7.7%** — 예상했던 20~25%가 아닙니다 |
| 치르는 것 | 문항 불일치가 잡음의 **7배**, 원인 미상 |

이 학원 규모에서 7.7%는 월 몇 천 원입니다. **그걸 사려고 원인 모르는
차이를 안고 갈 수는 없습니다.** 기본값은 `full`로 둡니다.

`items`·`compact` 선택지와 `variant` 축은 남겨둡니다 — 지운다고 알게 된 것이
없어지지 않고, 다음에 파이프라인을 건드릴 때 잴 자리가 필요합니다.

### 이 절에서 배운 것

세 가지가 남습니다.

1. **필드 이름을 줄여도 옮겨 적기는 안 나빠집니다**(4 < 9). 이건 확실합니다.
2. **작은 답안지에서는 스키마를 줄여도 별로 안 아낍니다.** 10문항짜리는
   시스템 프롬프트·이미지가 대부분이라 문항당 절감이 묻힙니다.
3. **한 번에 두 가지를 바꾸면 원인을 못 짚습니다.** 판정 입력의 들여쓰기를
   조건 밖에 둔 것과 겹쳐, 첫 실험의 해석이 통째로 흔들렸습니다.

### 절감폭도 예상보다 작았습니다

$0.882 → $0.794로 **10%**입니다. 예상은 20~25%였습니다. 이 7장 중 다섯 장이
10문항짜리라, 문항마다 아끼는 것이 시스템 프롬프트·이미지·머리말에 묻힙니다.
장이 클수록 큽니다 — 60문항인 김예진은 $0.273 → $0.238(**87%**),
10문항인 강이안은 $0.087 → $0.081(**93%**)이었습니다.

**정리하면 스키마에서 꺼낼 수 있는 돈은 10% 남짓이고, 그중 판정 몫은 못
씁니다.** 남는 것은 7% 안팎입니다.

### 안 한 것과 그 이유

| 후보 | 왜 안 했나 |
|---|---|
| 프롬프트 캐싱 | 시스템 프롬프트가 912자(~700토큰)로 **캐시 최소 단위 미만** |
| 두 쪽을 한 번에 | 아끼는 것은 시스템 프롬프트 한 벌(~2%)뿐이고 300초 한계에 가까워짐 |
| 이미지 해상도 낮추기 | **가장 큰 입력 항목**이지만 연필 판독이 걸려 있음. `/bench`가 저장된 사진을 다시 쓰므로 지금 구조로는 못 잽니다 — 재려면 서버에서 줄이는 길을 먼저 놔야 합니다 |
| `expected`를 오답에만 | 검수 때 선생님이 **정답 처리된 문항도 확인**합니다(§12.6의 철자 관대 채점). 빼면 검수 품질이 떨어집니다 |

---

## 13.22 🔴 규모를 세 자릿수 배 틀렸습니다 (2026-08-10)

> 원장: "내가 비용에 집착하는 이유를 알려줄게. 일주일 예상 판독 페이지 수:
> 총 650명 × 평균 4~5페이지 = 약 3000장. 한달로 치면 12,600장."

저는 그때까지 하루 일곱 장을 보고 있었습니다. §13.21에서 "7.7%면 월 몇 천
원이니 여기서 접자"고 권했는데, **실제로는 월 190만 원의 7.7%, 즉 22만
원이었습니다.**

권고를 뒤집습니다. 그리고 다시 그러지 않도록 **규모를 코드에 둡니다**
(`lib/cost.ts`).

### 실측에서 뽑은 비용 모형

비용이 붙는 자리는 둘이고, 답안지 크기가 10~60문항으로 흩어져 있어 가를 수
있었습니다.

```
비용 = 쪽수 × $0.0717  +  문항수 × $0.00173
        └ 사진+시스템 입력      └ 전사·판정 출력
```

2026-08-10 실측 일곱 장에 **오차 7% 안**으로 맞습니다(테스트로 묶어 뒀습니다).

### 월 12,600쪽이면

| 쪽당 문항 | 사진값 | 문항값 | 합계 | 원화 | 출력 몫 |
|---:|---:|---:|---:|---:|---:|
| 10 | $903 | $218 | $1,121 | **159만** | 19% |
| 20 | $903 | $436 | $1,339 | **190만** | 33% |
| 30 | $903 | $654 | $1,557 | **220만** | 42% |

**월 160~220만 원입니다.** 쪽당 문항 수만 알려주시면 좁혀집니다.

### 이 규모에서 길마다 얼마인가

| 길 | 월 절감 | 상태 |
|---|--:|---|
| **Batch API** (입·출력 반값) | **95만** | ❓ 안 재봄 — 아래 |
| Sonnet 5 | 76만 | ❌ 전사 22칸·43% 느림 |
| **이미지 해상도 절반** | **64만** | ❓ 잴 길을 안 놓음 |
| **학생당 1쪽 덜 찍기** | **41만** | ❓ 원장님 답이 필요 |
| 출력 스키마 압축 | 22만 | 🔶 원인 미상 차이 |

### 🔴 Batch는 못 씁니다 (원장님 확인, 2026-08-10)

아래에 "Batch를 잘못된 이유로 지웠다"고 적어놓고 여쭤봤습니다. 답은
**"학생이 가기 전에 알아야 한다"**였습니다.

즉시성이 **요구사항이 맞았습니다.** 제가 §13.19에서 한 줄로 지운 것은
근거 없이 단정한 것이 맞지만, 물어보니 결론은 같았습니다. 95만 원짜리
길은 닫혔습니다.

원본 시험지는 다음 날까지 학원에 있으므로 **사진을 다시 찍는 것은
가능합니다.** 사진 검사만 따로 붙일 이유는 없어졌습니다 — 지금처럼
80초 뒤에 "덜 찍혔다"가 뜨는 것으로 충분합니다.

**남은 큰 길은 이미지 해상도 하나입니다.**

### 그런데 그 값어치도 제가 산수로 냈습니다

"해상도를 반으로 줄이면 월 64만 원"은 `perPage`를 통째로 사진으로 친
값입니다. **사진이 입력의 몇 할인지 재본 적이 없습니다.**

토큰은 `sheets.token_usage`에 이미 다 있었습니다. `lib/tokens.ts`가
호출 순서로 단계를 가르고(마지막이 판정), 관리 화면에 띄웁니다.

| 화면이 답하는 것 | 왜 |
|---|---|
| 사진이 입력의 몇 % | **해상도**를 줄일 값어치 |
| 출력이 비용의 몇 % | **출력 스키마**를 줄일 값어치 |
| 실제 쪽당 문항 수 | 원장님도 확답 못 하신 값 — 앱이 셉니다 |
| 월 12,600쪽 환산 | 절감률이 아니라 **금액**으로 |

§13.22에서 "규모를 모르면 절감률은 의미가 없다"고 적어놓고 정작 **사진
몫도 모른 채** 다음 절감을 말하고 있었습니다. 이제 안 그럽니다.

---

### (원래 적었던 것) Batch를 잘못된 이유로 지웠습니다

§13.19에서 "조교가 접수하고 **기다리는** 흐름이라 못 씁니다"라고 적고 끝냈습니다.
**그건 지금 흐름에 대한 서술이지 요구사항이 아닙니다.**

Batch는 입력·출력 둘 다 반값이라 **혼자서 95만 원**입니다. 다른 모든 길을
합친 것과 맞먹습니다. 그리고 품질은 **한 글자도 안 바뀝니다** — 같은 모델,
같은 프롬프트, 같은 스키마입니다. 지금까지 잰 것 중 **품질을 안 건드리는
유일한 큰 절감**입니다.

써도 되는지는 코드가 아니라 **학원 운영이 정합니다.**

| 만약 | Batch |
|---|---|
| 재시험 명단이 **그날 밤**에 나오면 되면 | 쓸 수 있습니다 |
| 학생이 **가기 전에** 결과를 알아야 하면 | 그 반은 못 씁니다 |

반별로 갈라도 됩니다. 큐가 이미 있으니 "급한 것은 바로, 나머지는 밤에"가
구조상 어렵지 않습니다.

### 스키마 압축도 다시 봅니다

§13.21에서 7.7%가 나왔는데, **그 표본이 10문항짜리 다섯 장이었습니다.**
작은 답안지는 사진값이 지배해서 문항값을 줄여도 티가 안 납니다.

쪽당 30문항이면 출력이 비용의 42%이고, 압축은 그 위에 붙습니다.
**같은 변경이 표본에 따라 7%로도 16%로도 보입니다.** 재는 표본이 실제
답안지와 닮아야 한다는 뜻이고, 이건 §13.14("PASS만 있는 표본은 관대한
모델을 못 걸러낸다")와 같은 종류의 함정입니다.

### 남기는 교훈

**규모를 모르면 절감률은 의미가 없습니다.** "7%"는 결정을 못 내리는 숫자이고
"월 22만 원"은 내릴 수 있는 숫자입니다. 앞으로 절감을 말할 때는 **비율과
금액을 같이** 적습니다.

---

## 13.23 쌓여도 한 장은 그 장대로 (2026-08-10)

> 원장: "시험지가 계속 쌓일 경우, 앞에서 먼저 돌리기 시작한 채점의 속도에도
> 영향을 주는건지, 아니면 시험이 많이 쌓인 것과는 별개로 1명의 시험지 채점
> 시간은 동일한건지 알려줘."

**한 장의 채점 시간은 쌓인 양과 무관합니다.**

채점 한 장은 Vercel 함수 호출 하나이고, 그 안에서 Anthropic에 보내는 요청도
각각 독립입니다. 옆에서 세 장이 더 돌고 있어도 내 장은 내 장대로 끝납니다.
서로 자원을 나눠 쓰지 않습니다.

**늘어나는 것은 줄 서는 시간뿐입니다.**

### 줄이 생기는 지점

```
LANES = 4          동시에 네 장까지
```

다섯 번째 장은 앞의 넷 중 하나가 끝나야 시작합니다. 갈래는 **브라우저마다**
네 개라 조교 둘이 화면을 열어두면 여덟입니다.

| 한꺼번에 쌓인 장 | 마지막 것이 나오기까지 (4갈래·장당 150초) |
|---:|---:|
| 4장 이하 | **2분 반** — 줄이 안 생깁니다 |
| 10장 | 7분 반 |
| 20장 | **12분 반** |
| 20장 (조교 둘, 8갈래) | 6분 |

넘치기 시작하는 지점은 **분당 1.6명**입니다(4갈래 ÷ 150초). 원장님이 말씀하신
"A 먼저, B가 1분 뒤, D·E가 30초 뒤" 정도면 줄이 거의 안 생깁니다. 한 반이
끝나 스무 명이 몰릴 때만 걸립니다.

### 조교가 그걸 알아야 합니다

**"학생이 가기 전에 알아야 한다"**가 요구사항이 된 이상, 조교는 잡아둘지
보낼지를 정해야 합니다. 그런데 화면에는 `채점 중 3`만 떠 있었습니다 —
3분인지 15분인지 알 수가 없습니다.

접수 화면에 붙였습니다.

```
채점 중 4 · 대기 6 · 마지막 것 약 4분 · $1.20
```

한 장당 초는 **그날 실측의 중앙값**입니다. 평균은 60문항짜리 한 장에
끌려갑니다. 잰 게 없으면 **안 띄웁니다** — 틀린 대기 시간은 없는 것보다
나쁩니다. 한 번 어긋나면 그 뒤로 아무도 안 믿습니다.

돌고 있는 장은 **방금 시작한 것으로** 칩니다. 넉넉하게 말하는 쪽이
맞습니다 — 모자라게 말하면 학생을 이미 보낸 뒤에 결과가 나옵니다.

### 손잡이는 갈래 수 하나

`LANES`를 늘리면 대기가 그만큼 줄어듭니다. 코드 한 줄입니다.

**다만 공짜가 아닙니다.** 갈래를 늘리면 분당 요청이 늘고, 그건 Anthropic의
분당 한도에 걸립니다. 걸리면 429가 나고 **그때는 진짜로 느려집니다** —
재시도가 붙고, 재시도가 다 떨어지면 채점 실패로 기록됩니다.

지금 넷은 안전한 값이고, 실제 수업에서 대기가 문제가 되면 그때 올립니다.
**올리기 전에 한도부터 확인해야 합니다.**

---

## 13.24 실측이 제 모형을 뒤집었습니다 (2026-08-11)

관리 화면 「비용」이 처음 실제 숫자를 뱉었습니다. 2026-07-13 ~ 08-11, 13장.

```
16쪽 · 364문항 · $2.04
입력 156,158 · 출력 50,185
사진이 입력의 65% (쪽당 6,376토큰)
```

### 🔴 한 상자 안에서 두 숫자가 다른 말을 했습니다

| | 출력이 비용에서 |
|---|---|
| 실측 토큰 | **62%** |
| §13.22의 `쪽당 + 문항당` 모형 | **35%** |

그 모형은 답안지 **두 장**에 맞춘 것이었습니다. 화면이 실측과 모형을 나란히
띄우면서 서로 다른 말을 하고 있었고, 저는 **모형 쪽 숫자로 다음에 뭘 줄일지
정하고 있었습니다.**

맞추는 모형을 버렸습니다. 이제 **잰 토큰을 그대로 늘립니다.**

### 돈이 실제로 가는 곳

| | 토큰 | 비용 | 몫 | 줄이는 손잡이 |
|---|--:|--:|--:|---|
| 출력 | 50,185 | $1.255 | **62%** | 출력 스키마 |
| 이미지 입력 | 102,016 | $0.510 | **25%** | 해상도 |
| 그 외 입력 | 54,142 | $0.271 | 13% | (없음) |

**"사진이 입력의 65%"와 "사진이 비용의 25%"는 다른 말입니다.** 입력 자체가
비용의 38%뿐이기 때문입니다. 이 둘을 헷갈리면 해상도의 값어치를 두 배로
잡습니다 — 제가 §13.22에서 "월 64만"이라고 한 것이 그 실수입니다.

화면 문구도 「사진이 **입력**에서」에서 「사진이 **비용**에서」로 바꿨습니다.

### 사진은 축소되지 않고 있습니다

```
쪽당 6,376토큰 × 750 = 4.78M 픽셀
2576 × 1822 (A4 긴 변 2576) = 4.69M 픽셀
```

거의 정확히 맞습니다. **보낸 그대로 청구됩니다.** 긴 변을 줄이면 토큰이
넓이에 비례해 그대로 줄어듭니다.

### 월 12,600쪽이면 227만원

§13.22에서 198만원이라고 한 것도 그 모형 탓이었습니다. **잰 것을 그대로
늘리면 $1,603 ≈ 227만원**입니다.

| 길 | 월 절감 | 근거 |
|---|--:|---|
| 긴 변 2576→1288 | **43만** | 사진 토큰 ¼ |
| 긴 변 2576→1800 | **29만** | 사진 토큰 0.49배 |
| 출력 스키마 압축 | 17만 | §13.21 **실측** 7.6% |
| (사진을 아예 없애도) | 57만 | 해상도로 살 수 있는 천장 |

**순위가 뒤집혔습니다.** 스키마 압축이 아니라 해상도가 큰 쪽입니다.
그리고 출력이 62%인데 압축의 절감이 7.6%뿐인 이유는, 압축이 **전사 문항
출력에만** 붙고 머리말과 판정 출력은 그대로이기 때문입니다.

### 다음

해상도는 **연필이 읽히는지를 걸고 사는 것**이라 재고 나서 바꿔야 합니다.
지금 `/bench`는 저장된 사진을 그대로 다시 쓰므로 못 잽니다 — 서버에서
줄이는 길을 놔야 합니다.

29만~43만 원이면 그 길을 놓을 값어치가 있습니다.

---

## 13.25 사진 해상도를 잴 수 있게 (2026-08-11)

§13.24에서 남은 큰 길이 하나로 좁혀졌습니다.

| 길 | 월 절감 |
|---|--:|
| 긴 변 2576 → 1288 | **43만** |
| 긴 변 2576 → 1800 | **29만** |

그런데 잴 방법이 없었습니다. `/bench`는 저장된 사진을 그대로 다시 쓰므로
해상도를 바꿔 넣을 자리가 없었습니다.

### 실험용으로만 서버에서 줄입니다

```
저장된 사진 (2576)
   ↓  downscale()   ← 실험에서만
1800 또는 1288
   ↓
transcribe → 나머지는 전부 그대로
```

**실제 채점은 안 건드립니다.** 재서 통과하면 고칠 곳은 `lib/image.ts`의
`MAX_EDGE` — 접수할 때 브라우저가 줄이는 값입니다. 그러면 서버에서 줄일
일이 아예 없어지고, 업로드도 그만큼 빨라집니다.

### `sharp`를 화면과 한 파일에 두면 안 됩니다

처음에 상수·계산과 `sharp`를 한 파일에 뒀더니 **브라우저 묶음이 통째로
깨졌습니다.** `/bench`가 같은 파일에서 `EDGES`와 `tokenFactor`를 가져가는데
`sharp`는 네이티브 서버 모듈입니다.

| 파일 | 무엇 | 누가 씁니까 |
|---|---|---|
| `resize.ts` | 값 목록 · 크기 계산 · 토큰 배수 | 화면 **과** 서버 |
| `downscale.ts` | 실제로 줄이기 (`sharp`) | 서버만 |

### 🔴 토큰은 넓이에 붙습니다

긴 변을 **절반**으로 줄이면 토큰은 **4분의 1**입니다. 절반이 아닙니다.
화면 선택지에 배수를 같이 적어 헷갈리지 않게 했습니다.

```
원본 (2576)
2000px — 사진값 60%
1800px — 사진값 49%
1568px — 사진값 37%
1288px — 사진값 25%
```

`1568`을 넣은 것은 Anthropic이 한때 "이보다 크면 줄여서 처리한다"고
안내하던 값이기 때문입니다. **지금은 안 줄이고 그대로 청구합니다**(§13.24
실측). 그래도 그 언저리가 모델이 실제로 쓰는 해상도일 수 있어, 거기서
읽기가 안 나빠진다면 **지금 2576은 안 쓰이는 픽셀에 돈을 내는 것**이 됩니다.

### 무엇을 보고 판단하나

바뀌는 것은 **읽기**뿐입니다. 판정 규칙도 스키마도 그대로입니다.

| 재는 것 | 잡음 바닥 (2026-08-10, 7장) | 넘으면 |
|---|--:|---|
| **전사가 다른 칸** | **9** | 그 해상도는 못 씁니다 |
| 문항 불일치 | 1 / 147 | |
| 놓친 칸 | 0 | 칸을 통째로 못 읽기 시작한 것 |
| 여유 있는데 뒤집힘 | 0 | |

**연필이 안 읽히기 시작하는 지점을 찾는 것**이고, 그 바로 위에서 멈추면
됩니다. 위에서부터 내려오면 한 번에 하나씩만 바뀌므로 원인도 분명합니다
(§13.21에서 두 가지를 같이 바꿔 원인을 못 짚었습니다).

---

## 13.26 🔴 사진은 비용의 25%가 아니라 10%였습니다 (2026-08-11)

`2000px` 실험 결과입니다. 같은 7장, `Opus 5 · low`.

| | 잡음 바닥 | 2000px |
|---|--:|--:|
| 전사가 다른 칸 | 9 | **13** |
| 문항 불일치 | 1 / 147 | 7 / 147 |
| 놓친 칸 | 0 | 0 |
| 판정 뒤집힘 | 0 | 0 |
| **입력 토큰** | — | **91.0%** |
| 출력 토큰 | — | 97.9% |
| 비용 | 98.8% | **94.9%** |

### 토큰이 제 추정을 무너뜨렸습니다

사진 토큰을 0.603배로 줄였는데 **입력이 9%밖에 안 줄었습니다.**

```
9.0% = 사진 몫 × (1 − 0.603)
  ⇒ 사진은 입력의 23%       (관리 화면은 65%라고 했습니다)
  ⇒ 사진은 비용의 약 10%     (§13.24에서 25%라고 적었습니다)
```

**§13.24의 25%가 틀렸습니다.** 그 값은 전사 호출의 입력에서 시스템
프롬프트를 **750토큰으로 어림잡아** 뺀 나머지를 사진으로 친 것입니다.
어림이 낮으면 사진이 커 보이고, 실제로 그랬습니다.

§13.24에서 "산수 대신 실측"이라고 적어놓고 **어림값 하나를 섞어 넣은 것을
못 봤습니다.** 추정이 하나라도 들어가면 그건 실측이 아닙니다.

### 그래서 해상도의 천장이 내려앉았습니다

| | §13.24에서 말한 것 | 실측 |
|---|--:|--:|
| 사진을 아예 없애면 | 57만 | **23만** |
| 1288px | 43만 | **17만** |
| 2000px | 17만 | **11만** |

**사진을 통째로 없애도 월 23만 원이 한계**입니다. 그 절반쯤을 사려고
전사 흔들림을 1.4배로 지는 셈입니다.

### 저장된 사진 크기를 화면에 띄웁니다

추정을 걷어낼 수 있는 값이 이미 DB에 있었습니다 — `sheet_pages.width/height`.
**픽셀을 750으로 나누면 사진 토큰이고, 거기엔 어림이 안 들어갑니다.**

관리 화면의 「사진이 비용에서 N%」를 「저장된 사진 N px」로 바꿨습니다.
같은 자리에서 추정 대신 사실을 말합니다.

### 결론 — 해상도도 접습니다

2000px이 산 것은 **5.1%**이고, 치른 것은 전사 흔들림 9 → 13입니다.
1288px까지 내려도 천장이 17만 원인데, 거기서는 답안 한 칸이 20~40px이
됩니다 — `MAX_EDGE`를 2576으로 잡은 이유가 그 반대였습니다.

**돈으로 살 수 있는 것이 이제 남지 않았습니다.**

| 길 | 절감 | 결과 |
|---|--:|---|
| Batch API | 95만 | ❌ 결과가 즉시 필요 |
| Sonnet 5 | 76만 | ❌ 전사 22칸 · 43% 느림 |
| GPT-5.6 Terra | 140만 | ❌ 고쳐 읽기 |
| 해상도 2000px | 11만 | ❌ 전사 9 → 13 |
| 출력 스키마 압축 | 17만 | ❌ 원인 미상 차이 7배 |

**`claude-opus-5 · low` · 원본 해상도 · 지금 스키마가 재본 것 중 가장 나은
자리입니다.** 다섯 갈래를 다 재고 나온 결론이라, 이제 이건 짐작이 아닙니다.

월 227만 원은 **줄일 수 있는데 안 줄이고 있는 돈이 아니라, 이 품질의 값**입니다.

---

## 13.27 같은 시험을 30번 새로 읽지 않습니다 (2026-08-11)

> 원장: "왜 네 맘대로 비용이 정리됐다고 단언해? 다른 방법을 찾아봐."

맞는 지적이었습니다. §13.26까지 재본 다섯 가지는 전부 **"같은 일을 싸게
하기"**였습니다 — 싼 모델, 싼 해상도, 싼 스키마. 전부 품질과 맞바꾸는
자리였고 전부 탈락했습니다. 안 본 것은 **"같은 일을 반복하지 않기"**입니다.

### 반복이 어디 있었나

한 반 30명이 **같은 시험**을 봅니다. 그런데 학생마다

- 판정 모델이 **정답(expected)을 새로 만들어냈습니다.** 같은 문항의 정답을
  반에서 30번 생성한 것이고, 그래서 **같은 답을 쓴 두 학생이 다른 판정을
  받을 수 있었습니다.** 김예지 재실행에서 판정이 흔들리는 것을 실측으로
  봤습니다(§13.21) — 그건 비용 문제이기 전에 **공정성 문제**입니다.
- 빈칸·판독불가처럼 **판정 프롬프트에 이미 결정적 규칙이 있는** 문항도
  모델에 보냈습니다. 규칙이 결정적이면 코드가 맞습니다 — 모델은 가끔
  규칙을 어기고, 코드는 안 어깁니다.

### 시험 참조

첫 학생의 **깨끗한** 결과(밀림 경보 0 · 누락 0 · 정답 전부)를 그 시험의
참조로 남깁니다. `exam_refs` — 제시어와 정답뿐이라 **학생 정보가 없고**,
90일 삭제 대상도 아니며, 같은 교재를 다음 학기에 쓰면 그대로 씁니다.

같은 시험(정규화한 제목 + 문항 번호 전체가 지문)의 다음 답안지부터:

| | 지금까지 | 참조가 있으면 |
|---|---|---|
| 밀림 검사 | 전사 안에서만 | **+ 참조 제시어와 대조** (`checkDrift`의 `key` — 원래 설계의 자리) |
| 빈칸·판독불가 | 모델이 판정 | **코드가 판정** (프롬프트의 규칙 그대로) |
| 정답과 같은 답 | 모델이 판정 | **코드가 정답 처리** |
| 나머지 | 모델이 정답을 만들며 판정 | 모델이 **주어진 정답으로** 판정 (`judgeWithKey`) |

**자동 판정은 정답 쪽으로만 기웁니다.** 참조와 다르다고 오답 처리하지
않습니다 — en2ko는 같은 뜻의 다른 표기가 정답이고(§12.6), 철자 관대
채점도 있습니다. 다른 답은 전부 모델이 봅니다. 그리고 **제시어가 참조와
다른 칸은 자동 정답 처리에서 뺍니다** — 밀린 칸의 '정답 일치'는 옆 문항
답이 우연히 맞은 것일 수 있고, 그 칸은 밀림 경보가 사람에게 보냅니다.

### 지키는 것들

| 위험 | 대응 |
|---|---|
| 틀린 참조가 반 전체로 번짐 | 깨끗한 답안지에서만 생성 · **전 장 검수 유지** · 검수 화면의 정답 칸이 참조 값 |
| 조교 둘이 동시에 첫 장을 채점 | **먼저 저장된 참조가 이깁니다** — 반 중간에 기준이 안 바뀝니다 |
| 제목 전사 흔들림으로 참조를 놓침 | 놓치면 전체 경로로 갈 뿐 — **틀린 참조보다 없는 참조가 낫습니다** |
| 마이그레이션 지연 | 표가 없으면 참조 없이, 칸이 없으면 끈 것으로 — 지금까지와 동일 동작 |

### 🔴 기본 꺼짐

실제 채점 동작이 바뀌므로 **관리 화면의 스위치**로 관리자가 켭니다.
끄면 즉시 예전 방식입니다. `/bench`·빠른 시험은 참조를 안 씁니다 —
실험은 계속 기본 경로를 재야 조건이 안 섞입니다.

### 예상 절감

판정 비용(전체의 ~17%)에서: 정답 재생성 소멸 + 정답 일치·빈칸 문항의
판정 소멸. 반 크기가 클수록, 시험이 반복될수록 커집니다. 추정 **월
20~30만 원**이고, §13.22의 교훈대로 **켠 뒤 관리 화면 「비용」에서 실측**으로
확인합니다 — 추정을 결론으로 삼지 않습니다.

---

## 13.28 남은 둘 — 철회 삭제와 프롬프트 캐싱 (2026-08-11)

### 동의 철회 삭제 (docs/14 §14.7 완결)

동의서에 "철회 시 7일 안에 파기"라고 적어 **이미 돌렸습니다.** 지우는 코드가
없으면 그 문장은 거짓말입니다. 관리 화면에 「동의 철회」를 붙였습니다 —
자세한 것은 [14 §14.7](14-consent.md).

- 이름이 **정확히 같은** 답안지만 찾습니다. 부분 일치면 '김예진'을 지우다
  '김예진아'가 걸립니다. 동명이인은 반·날짜를 보고 사람이 가립니다.
- **행째** 지웁니다. 사진만 지우고 전사를 남기면 학생이 쓴 답과 이름이
  남아 파기가 아닙니다.
- 순서는 사진 먼저(`deleteSheet` 그대로). 끊기면 사진 없는 행이 남을
  뿐이고, 반대 순서면 "지웠다"는 기록만 남고 사진이 남습니다.

### 프롬프트 캐싱 — 글자 하나 안 바꾸고

시스템 프롬프트에 캐시 표시(`cache_control`)만 붙였습니다. §13.27의 [B]인데,
처음 생각(1,024토큰까지 채워넣기)은 **버렸습니다** — 채워넣기는 프롬프트
변경이고, 프롬프트 변경은 재봐야 합니다(§13.21). 표시만 붙이는 것은 잴 것이
없습니다: 최소 길이를 넘으면 캐시되고, 못 넘으면 아무 일도 안 일어납니다.

**따라온 것이 더 중요했습니다.** 캐시가 걸리면 그 몫이 `input_tokens`에서
**빠져서** 옵니다. 그대로 두면 캐싱이 동작하는 날부터 비용 화면이 실제보다
싸게 말합니다 — 이번 주 내내 만든 화면이 조용히 거짓말을 시작하는 것입니다.
`Usage`에 `cacheRead`/`cacheWrite`를 적고 `costUsd`가 읽기 10%·쓰기 125%로
따로 셉니다. 회귀 테스트로 묶었습니다.

### 볼륨 할인 — 코드 없는 마지막 길

월 $1,600 규모면 Anthropic 영업 상담 대상입니다. support.claude.com 또는
sales@anthropic.com 으로 문의합니다. 문안은 원장님께 따로 드렸습니다.

---

## 13.29 해상도 종결 — 품질이 끝냈습니다 (2026-08-11)

`1288px` 실측 (같은 7장):

| | 원본 | 2000px | **1288px** |
|---|--:|--:|--:|
| 전사가 다른 칸 (잡음 9) | 9 | 13 | **19** |
| 판정 | — | 전부 같음 | **김예진 PASS→FAIL** (경계선 장) |
| 비용 | 100% | 94.9% | 74.9% |

**읽기가 해상도를 따라 단조로 나빠집니다: 9 → 13 → 19.** 1288에서는
문항 정오도 7건 갈렸고(김민석 +9 등), 커트라인에 걸려 있던 김예진이
뒤집혔습니다. 경계선 장이라 모델 흠으로 세지는 않지만, 방향은 분명합니다.
**중간 값(1800·1568)도 13과 19 사이일 것이므로 잴 이유가 없습니다.**

→ **해상도는 품질 근거로 영구 종결. 2576 유지.**

### 청구 방식 수수께끼는 남습니다 — 그리고 안 쫓습니다

2000px에서 입력이 7k만 떨어졌는데 1288px에서는 51k가 떨어졌습니다.
사진이 픽셀 그대로 청구된다면 둘 다 그 비율로 떨어졌어야 합니다. 서버가
큰 사진을 어느 선에서 줄여 처리하는 것으로 보이지만, **해상도가 품질로
닫힌 이상 이 수수께끼를 돈 내고 풀 이유가 없습니다.** §13.24의 "보낸
그대로 청구된다"는 단정은 취소합니다 — 근거가 어림값이었습니다.

### 부산물 — 캐싱이 실제로 걸렸습니다

이 실행은 캐싱 배포(§13.28) **이후**의 첫 실험이었습니다. 증거가 둘:

1. 절감액($0.221)이 입력 감소분의 정가($0.255)보다 **작습니다** —
   일부 입력이 10%/125% 단가로 청구됐다는 뜻입니다.
2. 입력 감소(66%)가 사진 감소만으로 설명이 안 됩니다 — 시스템 프롬프트가
   캐시로 빠져 `input_tokens`에서 사라졌습니다.

즉 **시스템 프롬프트가 최소 길이(1,024토큰)를 넘고, 캐싱은 공짜로 돌고
있습니다.** 원본 해상도에서도 똑같이 적용됩니다.

여기서 계측 구멍도 하나 잡았습니다: `/bench`의 입력 줄이 캐시된 토큰을
빼고 세고 있었습니다. 캐싱 배포 전의 기준과 후의 실험을 비교하면 입력이
실제보다 더 줄어 보입니다 — **한 실험에 두 변수가 또 섞인 것입니다.**
캐시 포함으로 고쳤습니다.

### 비용 조사 최종 상태

| 길 | 판정 |
|---|---|
| 모델 교체 (Sonnet·Haiku·GPT) | ❌ 읽기 |
| Batch | ❌ 즉시성 |
| 출력 스키마 | ❌ 원인 미상 차이 |
| 해상도 | ❌ 읽기 (9→13→19) |
| 볼륨 할인 | ❌ 존재하지 않음 (영업 확인) |
| **시험 참조** | 🟢 켜면 절감 + 일관성 (스위치 대기) |
| **캐싱** | 🟢 **이미 돌고 있음** — 공짜 |

`claude-opus-5 · low · 2576px · 원래 스키마` + 참조 + 캐싱.
**여기가 종착지이고, 전부 실측으로 확인된 자리입니다.**

---

## 13.30 캐싱 실측 — 11% (2026-08-11)

기준과 **완전히 같은 조건**(Opus 5 · low · 2576 · 지금 형식)으로 같은 7장을
다시 돌렸습니다. 다른 것은 캐싱 배포 전후뿐입니다.

| | 기준 (캐싱 전) | 재실행 (캐싱 후) |
|---|--:|--:|
| 입력 토큰 (캐시 포함) | 77,496 | **77,490 = 100.0%** |
| 출력 토큰 | 19,794 | 19,257 (잡음) |
| 비용 | $0.882 | **$0.785 = 89.0%** |

**입력이 완전히 같은데 비용만 11% 내렸습니다** — 캐싱 말고는 설명이 없고,
입력 100.0%는 비교가 공정했다는 확인이기도 합니다.

절감에서 출력 잡음을 걷어내면 캐시된 토큰이 **호출당 약 2,060**으로
역산됩니다. 시스템 프롬프트(~1,000)만이 아니라 **구조화 출력 스키마까지
프리픽스로 같이 캐시되는** 크기입니다 — 예상(5%)의 두 배가 나온 이유입니다.

규모로 옮기면 **월 227만 → 약 202만 원.** 다만 이 실험은 호출이 연달아
이어진 조건(5분 TTL 안에서 계속 적중)이라 **바쁜 시간의 상한**입니다.
한가한 시간대의 드문 호출은 이보다 덜 맞습니다. 실제 값은 수업이 돌기
시작하면 관리 「비용」에 그대로 쌓입니다.

### 3번·14번은 원래 흔들리는 문항이었습니다

이 동일 조건 재실행에서 김예지의 **3번·14번**이 또 갈렸습니다. §13.21의
압축 실험 두 번에서 갈렸던 바로 그 번호들입니다. 즉 그때 "원인 미상"으로
남겨둔 차이의 일부는 스키마 탓이 아니라 **그 문항들 자체가 판정 경계에
있는 것**이었습니다. 문항 단위 잡음 바닥은 1/147이 아니라 **~2/147이고,
같은 번호가 반복해서 흔들립니다.** 앞으로 벤치를 읽을 때 이 둘은 빼고
봐야 합니다.

김예지가 판정을 못 낸 것도 세 번째 — 그 답안지의 성질이라는 결론이 한 번
더 확인됐습니다.

---

## 13.31 셀프 가입 + 관리자 승인 (2026-08-11)

> 원장: "사용자가 직접 계정을 만들어서 계정승인 신청을 넣는 구조로 바꿀 수
> 있을까?"

바꿨습니다. 그리고 **승인이라는 새 개념을 만들지 않았습니다** — 비활성
직원 행이 곧 신청서입니다.

```
조교가 가입 (이름·이메일·비밀번호)
   ↓  자기 행을 active=false · role='assistant'로 넣음
관리자: People에 "꺼진 계정 N개" 안내
   ↓  켜기 = 승인   (역할 승격도 같은 자리)
조교 화면이 열림
```

### 왜 안전한가

- `is_staff()`·`is_admin()`·`can_confirm()`이 전부 `active`를 요구합니다
  (§13.17). **승인 전 계정은 답안지·사진·채점 어디에도 못 닿습니다.**
  주소를 아는 누군가가 가입해도 꺼진 행 하나가 생길 뿐입니다.
- 자기 행 insert는 RLS가 **`active=false`·`role='assistant'`일 때만**
  받습니다. 스스로 승인하거나 관리자로 가입하는 길은 DB가 막습니다.
- 자기 행은 읽을 수 있습니다(`staff_read_self`). 없으면 "승인 대기 중"과
  "명부에 없음"을 화면이 못 가릅니다. `auth.uid()` 비교뿐이라 §13.10의
  재귀와 무관합니다.

### 화면

- 로그인 카드에 **"처음입니다 — 계정 만들기"** 가 생겼습니다.
- 가입 직후(또는 확인 메일을 거쳐 로그인하면) **승인 대기 화면**이 뜹니다.
  "승인됐는지 확인" 단추만 있고, 다른 것은 아무것도 없습니다.
- 대기와 사용 중지는 **같은 화면**입니다. "당신은 잘렸습니다"를 화면이
  단정할 이유가 없고, 어느 쪽이든 다음 행동은 원장님께 문의입니다.
- 관리자가 만들어주는 기존 방식(`/api/staff`)도 그대로 남아 있습니다.

### 관리자가 할 일이 하나 늘었습니다

People의 꺼진 계정 안내에 적어두었습니다: **모르는 이름이면 켜지 말 것.**
가입 신청까지는 누구나 할 수 있으므로, 승인은 아는 사람에게만 합니다.
